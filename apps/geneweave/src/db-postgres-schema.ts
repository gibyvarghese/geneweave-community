// SPDX-License-Identifier: MIT
// AUTO-GENERATED by scripts/gen-postgres-schema.ts from the live SQLite schema. DO NOT EDIT BY HAND.
// Regenerate with: npx tsx scripts/gen-postgres-schema.ts
// 262 tables, mirroring the fully-migrated SQLite schema for the Postgres adapter.

/** The complete geneWeave app schema in Postgres DDL, at type/row parity with SQLite. */
export const POSTGRES_FULL_SCHEMA = `
CREATE TABLE IF NOT EXISTS "a2a_skills" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT NOT NULL DEFAULT '',
  "tags" TEXT,
  "examples" TEXT,
  "input_modes" TEXT,
  "output_modes" TEXT,
  "security_scopes" TEXT NOT NULL DEFAULT '["a2a:chat"]',
  "mode" TEXT NOT NULL DEFAULT 'agent',
  "required_permission" TEXT,
  "sort_order" BIGINT NOT NULL DEFAULT 0,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "agent_tools" TEXT DEFAULT NULL,
  "agent_workers" TEXT DEFAULT NULL,
  "agentic_scope" TEXT NOT NULL DEFAULT 'system',
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "a2a_tasks" (
  "id" TEXT,
  "context_id" TEXT NOT NULL,
  "status_state" TEXT NOT NULL DEFAULT 'TASK_STATE_SUBMITTED',
  "status_timestamp" TEXT NOT NULL,
  "status_message" TEXT,
  "artifacts" TEXT NOT NULL DEFAULT '[]',
  "history" TEXT NOT NULL DEFAULT '[]',
  "metadata" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "agenda_categories" (
  "id" TEXT,
  "tenant_id" TEXT,
  "user_id" TEXT,
  "name" TEXT NOT NULL,
  "color" TEXT NOT NULL DEFAULT '#7C5CFC',
  "icon" TEXT NOT NULL DEFAULT '◆',
  "template_key" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "agenda_items" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "title" TEXT NOT NULL,
  "kind" TEXT NOT NULL DEFAULT 'event',
  "category_id" TEXT,
  "start_at" TEXT,
  "end_at" TEXT,
  "all_day" BIGINT NOT NULL DEFAULT 0,
  "location" TEXT,
  "description" TEXT,
  "recurrence_rule" TEXT,
  "status" TEXT NOT NULL DEFAULT 'confirmed',
  "sensitivity" TEXT NOT NULL DEFAULT 'normal',
  "amount" TEXT,
  "currency" TEXT,
  "provenance" TEXT,
  "linked_task_id" TEXT,
  "linked_run_id" TEXT,
  "linked_note_id" TEXT,
  "parent_item_id" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "tenants" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "parent_tenant_id" TEXT,
  "path" TEXT NOT NULL,
  "depth" BIGINT NOT NULL DEFAULT 0,
  "status" TEXT NOT NULL DEFAULT 'active',
  "metadata" TEXT NOT NULL DEFAULT '{}',
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("parent_tenant_id") REFERENCES "tenants" ("id")
);

CREATE TABLE IF NOT EXISTS "users" (
  "id" TEXT,
  "email" TEXT NOT NULL,
  "name" TEXT NOT NULL,
  "persona" TEXT NOT NULL DEFAULT 'tenant_user',
  "tenant_id" TEXT,
  "password_hash" TEXT NOT NULL,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "email_bidx" TEXT,
  "email_verified" BIGINT NOT NULL DEFAULT 0,
  "email_verified_at" TEXT,
  "mfa_enabled" BIGINT NOT NULL DEFAULT 0,
  "mfa_totp_secret" TEXT,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("tenant_id") REFERENCES "tenants" ("id") ON DELETE SET NULL
);

CREATE TABLE IF NOT EXISTS "chats" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "title" TEXT NOT NULL DEFAULT 'New Chat',
  "model" TEXT NOT NULL DEFAULT '',
  "provider" TEXT NOT NULL DEFAULT '',
  "pinned" BIGINT NOT NULL DEFAULT 0,
  "archived" BIGINT NOT NULL DEFAULT 0,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("user_id") REFERENCES "users" ("id")
);

CREATE TABLE IF NOT EXISTS "agent_checkpoints" (
  "run_id" TEXT,
  "chat_id" TEXT NOT NULL,
  "user_id" TEXT NOT NULL,
  "agent_name" TEXT NOT NULL,
  "step_index" BIGINT NOT NULL DEFAULT 0,
  "payload" TEXT NOT NULL,
  "status" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD"T"HH24:MI:SS.MS"Z"'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD"T"HH24:MI:SS.MS"Z"'),
  PRIMARY KEY ("run_id"),
  FOREIGN KEY ("chat_id") REFERENCES "chats" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "agent_compliance_audit" (
  "id" TEXT,
  "chat_id" TEXT NOT NULL,
  "user_id" TEXT NOT NULL,
  "run_id" TEXT NOT NULL,
  "agent_name" TEXT NOT NULL,
  "tool_name" TEXT NOT NULL,
  "subject_id" TEXT,
  "purpose" TEXT,
  "data_classification" TEXT,
  "outcome" TEXT NOT NULL,
  "denied_reason" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD"T"HH24:MI:SS.MS"Z"'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("chat_id") REFERENCES "chats" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "agent_cost_ledger" (
  "id" TEXT,
  "chat_id" TEXT NOT NULL,
  "user_id" TEXT NOT NULL,
  "run_id" TEXT NOT NULL,
  "agent_id" TEXT NOT NULL,
  "model_id" TEXT NOT NULL,
  "prompt_tokens" BIGINT NOT NULL DEFAULT 0,
  "completion_tokens" BIGINT NOT NULL DEFAULT 0,
  "total_tokens" BIGINT NOT NULL DEFAULT 0,
  "cost_usd" DOUBLE PRECISION,
  "called_at" TEXT NOT NULL,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD"T"HH24:MI:SS.MS"Z"'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("chat_id") REFERENCES "chats" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "agent_eval_pipeline_runs" (
  "id" TEXT,
  "chat_id" TEXT NOT NULL,
  "user_id" TEXT NOT NULL,
  "run_id" TEXT NOT NULL,
  "agent_name" TEXT NOT NULL,
  "report" TEXT NOT NULL,
  "accepted" BIGINT NOT NULL DEFAULT 1,
  "overall_score" DOUBLE PRECISION NOT NULL DEFAULT 1.0,
  "revisions" BIGINT NOT NULL DEFAULT 0,
  "verify_attempts" BIGINT NOT NULL DEFAULT 0,
  "evaluated_at" TEXT NOT NULL,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD"T"HH24:MI:SS.MS"Z"'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("chat_id") REFERENCES "chats" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "agent_graph_edges" (
  "id" TEXT,
  "chat_id" TEXT NOT NULL,
  "user_id" TEXT NOT NULL,
  "source_entity_id" TEXT NOT NULL,
  "target_entity_id" TEXT NOT NULL,
  "relationship_type" TEXT NOT NULL,
  "weight" DOUBLE PRECISION NOT NULL DEFAULT 1.0,
  "properties_json" TEXT NOT NULL DEFAULT '{}',
  "edge_created_at" BIGINT NOT NULL,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD"T"HH24:MI:SS.MS"Z"'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("chat_id") REFERENCES "chats" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "agent_graph_nodes" (
  "id" TEXT,
  "chat_id" TEXT NOT NULL,
  "user_id" TEXT NOT NULL,
  "entity_id" TEXT NOT NULL,
  "entity_type" TEXT NOT NULL,
  "entity_name" TEXT NOT NULL,
  "properties_json" TEXT NOT NULL DEFAULT '{}',
  "entity_created_at" BIGINT NOT NULL,
  "entity_updated_at" BIGINT NOT NULL,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD"T"HH24:MI:SS.MS"Z"'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD"T"HH24:MI:SS.MS"Z"'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("chat_id") REFERENCES "chats" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "agent_handoff_log" (
  "id" TEXT,
  "chat_id" TEXT NOT NULL,
  "from_agent" TEXT NOT NULL,
  "to_agent" TEXT NOT NULL,
  "transfer_input" TEXT NOT NULL DEFAULT '',
  "result_status" TEXT,
  "result_output" TEXT,
  "started_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD"T"HH24:MI:SS.MS"Z"'),
  "completed_at" TEXT,
  "duration_ms" BIGINT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD"T"HH24:MI:SS.MS"Z"'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("chat_id") REFERENCES "chats" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "agent_output_schemas" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT NOT NULL DEFAULT '',
  "schema_type" TEXT NOT NULL DEFAULT 'json_object',
  "schema_json" TEXT,
  "strict" BIGINT NOT NULL DEFAULT 0,
  "chat_id" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("chat_id") REFERENCES "chats" ("id") ON DELETE SET NULL
);

CREATE TABLE IF NOT EXISTS "agent_plan_cache_config" (
  "id" TEXT DEFAULT 'global',
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "similarity_threshold" DOUBLE PRECISION NOT NULL DEFAULT 0.86,
  "min_steps" BIGINT NOT NULL DEFAULT 2,
  "max_entries" BIGINT NOT NULL DEFAULT 1000,
  "ttl_ms" BIGINT NOT NULL DEFAULT 86400000,
  "scope" TEXT NOT NULL DEFAULT 'user',
  "embedding_model" TEXT NOT NULL DEFAULT 'text-embedding-3-small',
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "agent_scopes" (
  "id" TEXT,
  "display_name" TEXT NOT NULL,
  "description" TEXT NOT NULL DEFAULT '',
  "sandboxed" BIGINT NOT NULL DEFAULT 1,
  "max_delegation_depth" BIGINT NOT NULL DEFAULT 5,
  "audit_level" TEXT NOT NULL DEFAULT 'log',
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "agent_strategy_settings" (
  "id" TEXT,
  "scope" TEXT NOT NULL DEFAULT 'global',
  "tenant_id" TEXT,
  "reflect_enabled" BIGINT NOT NULL DEFAULT 0,
  "reflect_max_revisions" BIGINT NOT NULL DEFAULT 1,
  "reflect_criteria" TEXT,
  "verify_enabled" BIGINT NOT NULL DEFAULT 0,
  "verify_min_score" DOUBLE PRECISION NOT NULL DEFAULT 0.7,
  "verify_max_attempts" BIGINT NOT NULL DEFAULT 1,
  "supervisor_replan_on_failure" BIGINT NOT NULL DEFAULT 0,
  "supervisor_parallel_delegation" BIGINT NOT NULL DEFAULT 0,
  "ensemble_resolver" TEXT,
  "a2a_enabled" BIGINT NOT NULL DEFAULT 0,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "parallel_tool_calls" BIGINT NOT NULL DEFAULT 1,
  "context_strategy" TEXT,
  "context_max_tokens" BIGINT,
  "context_window_size" BIGINT NOT NULL DEFAULT 20,
  "tool_retry_max_attempts" BIGINT NOT NULL DEFAULT 0,
  "tool_retry_backoff_ms" BIGINT NOT NULL DEFAULT 200,
  "tool_retry_max_backoff_ms" BIGINT NOT NULL DEFAULT 10000,
  "hitl_threshold" DOUBLE PRECISION NOT NULL DEFAULT 0.75,
  "max_agent_hops" BIGINT NOT NULL DEFAULT 5,
  "tool_confirmation_level" TEXT NOT NULL DEFAULT 'high-risk-only',
  "memory_policy" TEXT NOT NULL DEFAULT 'session',
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "agent_structured_outputs" (
  "id" TEXT,
  "chat_id" TEXT NOT NULL,
  "message_id" TEXT,
  "agent_name" TEXT NOT NULL DEFAULT '',
  "schema_name" TEXT,
  "output_json" TEXT NOT NULL,
  "retry_count" BIGINT NOT NULL DEFAULT 0,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("chat_id") REFERENCES "chats" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "agents" (
  "id" TEXT,
  "tenant_id" TEXT,
  "category" TEXT NOT NULL DEFAULT 'general',
  "name" TEXT NOT NULL,
  "display_name" TEXT,
  "description" TEXT,
  "system_prompt" TEXT,
  "include_utility_tools" BIGINT NOT NULL DEFAULT 1,
  "default_timezone" TEXT,
  "is_default" BIGINT NOT NULL DEFAULT 0,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "default_task_type" TEXT,
  "allowed_task_types" TEXT,
  "preferred_models" TEXT,
  "cost_ceiling_per_call" DOUBLE PRECISION,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "agent_tools" (
  "agent_id" TEXT NOT NULL,
  "tool_name" TEXT NOT NULL,
  "allocation" TEXT NOT NULL DEFAULT 'default',
  PRIMARY KEY ("agent_id", "tool_name"),
  FOREIGN KEY ("agent_id") REFERENCES "agents" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "api_live_runs" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "agent_id" TEXT,
  "status" TEXT NOT NULL DEFAULT 'running',
  "stop_requested" BIGINT NOT NULL DEFAULT 0,
  "config_json" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "artifact_policies" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "max_size_bytes" BIGINT,
  "allowed_types" TEXT,
  "retention_days" BIGINT,
  "require_versioning" BIGINT NOT NULL DEFAULT 1,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "artifacts" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "type" TEXT NOT NULL,
  "mime_type" TEXT NOT NULL,
  "data_text" TEXT,
  "data_blob" BYTEA,
  "size_bytes" BIGINT,
  "version" BIGINT NOT NULL DEFAULT 1,
  "session_id" TEXT,
  "user_id" TEXT,
  "agent_id" TEXT,
  "run_id" TEXT,
  "tags" TEXT,
  "metadata" TEXT,
  "policy_id" TEXT,
  "scope" TEXT NOT NULL DEFAULT 'session',
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT,
  "streaming_status" TEXT DEFAULT NULL,
  "streaming_progress" DOUBLE PRECISION DEFAULT NULL,
  "tenant_id" TEXT DEFAULT NULL,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("policy_id") REFERENCES "artifact_policies" ("id") ON DELETE SET NULL
);

CREATE TABLE IF NOT EXISTS "artifact_versions" (
  "id" TEXT,
  "artifact_id" TEXT NOT NULL,
  "version" BIGINT NOT NULL,
  "data_text" TEXT,
  "data_blob" BYTEA,
  "changelog" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("artifact_id") REFERENCES "artifacts" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "audit_log_retention_tiers" (
  "tier" TEXT,
  "retention_days" BIGINT NOT NULL,
  "description" TEXT NOT NULL,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("tier")
);

CREATE TABLE IF NOT EXISTS "cache_invalidation_rules" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "trigger" TEXT NOT NULL,
  "pattern" TEXT,
  "config" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "cache_metrics" (
  "window_start" TEXT,
  "response_hits" BIGINT NOT NULL DEFAULT 0,
  "response_misses" BIGINT NOT NULL DEFAULT 0,
  "prompt_cache_read_tokens" BIGINT NOT NULL DEFAULT 0,
  "prompt_cache_write_tokens" BIGINT NOT NULL DEFAULT 0,
  "cost_saved_usd" DOUBLE PRECISION NOT NULL DEFAULT 0,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("window_start")
);

CREATE TABLE IF NOT EXISTS "cache_policies" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "scope" TEXT NOT NULL DEFAULT 'global',
  "ttl_ms" BIGINT NOT NULL DEFAULT 300000,
  "max_entries" BIGINT NOT NULL DEFAULT 1000,
  "max_bytes" BIGINT NOT NULL DEFAULT 0,
  "bypass_patterns" TEXT,
  "output_bypass_patterns" TEXT,
  "invalidate_on" TEXT,
  "key_hashing" TEXT NOT NULL DEFAULT 'sha256',
  "tenant_isolation" BIGINT NOT NULL DEFAULT 1,
  "cache_temperature_gate" DOUBLE PRECISION NOT NULL DEFAULT 0,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "swr_ms" BIGINT NOT NULL DEFAULT 0,
  "negative_ttl_ms" BIGINT NOT NULL DEFAULT 0,
  "eviction_policy" TEXT NOT NULL DEFAULT 'lru',
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "cache_settings" (
  "id" TEXT DEFAULT 'global',
  "l2_enabled" BIGINT NOT NULL DEFAULT 0,
  "l2_provider" TEXT NOT NULL DEFAULT 'none',
  "l1_max_entries" BIGINT NOT NULL DEFAULT 5000,
  "l1_max_bytes" BIGINT NOT NULL DEFAULT 0,
  "l1_ttl_ms" BIGINT NOT NULL DEFAULT 30000,
  "key_namespace" TEXT NOT NULL DEFAULT 'weave:cache',
  "global_version_token" TEXT NOT NULL DEFAULT 'v1',
  "stampede_protection" BIGINT NOT NULL DEFAULT 0,
  "metrics_enabled" BIGINT NOT NULL DEFAULT 1,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "l1_eviction_policy" TEXT NOT NULL DEFAULT 'lru',
  "l1_negative_ttl_ms" BIGINT NOT NULL DEFAULT 0,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "capability_pack_experiments" (
  "id" TEXT,
  "pack_key" TEXT NOT NULL,
  "name" TEXT NOT NULL,
  "variants" TEXT NOT NULL,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "capability_packs" (
  "id" TEXT,
  "pack_key" TEXT NOT NULL,
  "version" TEXT NOT NULL,
  "status" TEXT NOT NULL DEFAULT 'draft',
  "name" TEXT NOT NULL,
  "description" TEXT NOT NULL DEFAULT '',
  "authored_by" TEXT,
  "manifest" TEXT NOT NULL,
  "installed_at" TEXT,
  "installed_by" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "capability_pack_installations" (
  "id" TEXT,
  "pack_id" TEXT NOT NULL,
  "pack_key" TEXT NOT NULL,
  "pack_version" TEXT NOT NULL,
  "ledger" TEXT NOT NULL,
  "installed_by" TEXT,
  "installed_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "uninstalled_at" TEXT,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("pack_id") REFERENCES "capability_packs" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "capability_policy_bindings" (
  "id" TEXT,
  "binding_kind" TEXT NOT NULL,
  "binding_ref" TEXT NOT NULL,
  "policy_kind" TEXT NOT NULL,
  "policy_ref" TEXT NOT NULL,
  "precedence" BIGINT NOT NULL DEFAULT 0,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "chat_settings" (
  "chat_id" TEXT,
  "mode" TEXT NOT NULL DEFAULT 'agent',
  "system_prompt" TEXT,
  "timezone" TEXT,
  "enabled_tools" TEXT,
  "redaction_enabled" BIGINT NOT NULL DEFAULT 1,
  "redaction_patterns" TEXT,
  "workers" TEXT,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "reflect_enabled" BIGINT NOT NULL DEFAULT 0,
  "reflect_max_revisions" BIGINT NOT NULL DEFAULT 1,
  "reflect_criteria" TEXT,
  "verify_enabled" BIGINT NOT NULL DEFAULT 0,
  "verify_min_score" DOUBLE PRECISION NOT NULL DEFAULT 0.7,
  "verify_max_attempts" BIGINT NOT NULL DEFAULT 1,
  "supervisor_replan_on_failure" BIGINT NOT NULL DEFAULT 0,
  "supervisor_parallel_delegation" BIGINT NOT NULL DEFAULT 0,
  "ensemble_agents" TEXT,
  "ensemble_resolver" TEXT,
  "parallel_tool_calls" BIGINT NOT NULL DEFAULT 1,
  "context_strategy" TEXT,
  "context_max_tokens" BIGINT,
  "context_window_size" BIGINT NOT NULL DEFAULT 20,
  "tool_retry_max_attempts" BIGINT NOT NULL DEFAULT 0,
  "tool_retry_backoff_ms" BIGINT NOT NULL DEFAULT 200,
  "tool_retry_max_backoff_ms" BIGINT NOT NULL DEFAULT 10000,
  "hitl_enabled" BIGINT NOT NULL DEFAULT 0,
  "hitl_require_all" BIGINT NOT NULL DEFAULT 0,
  "hitl_timeout_ms" BIGINT NOT NULL DEFAULT 300000,
  "handoffs_enabled" BIGINT NOT NULL DEFAULT 0,
  "graph_enabled" BIGINT NOT NULL DEFAULT 0,
  "graph_max_nodes" BIGINT NOT NULL DEFAULT 500,
  "graph_persist_enabled" BIGINT NOT NULL DEFAULT 1,
  "memory_context_enabled" BIGINT NOT NULL DEFAULT 0,
  "memory_context_max_chars" BIGINT NOT NULL DEFAULT 4000,
  "checkpoint_enabled" BIGINT NOT NULL DEFAULT 0,
  "checkpoint_interval_steps" BIGINT NOT NULL DEFAULT 1,
  "dynamic_workers_enabled" BIGINT NOT NULL DEFAULT 0,
  "max_dynamic_workers" BIGINT NOT NULL DEFAULT 20,
  "eval_pipeline_enabled" BIGINT NOT NULL DEFAULT 0,
  "eval_pipeline_stages" TEXT,
  "eval_pipeline_fail_fast" BIGINT NOT NULL DEFAULT 1,
  "cost_governor_enabled" BIGINT NOT NULL DEFAULT 0,
  "cost_governor_policy" TEXT,
  "compliance_enabled" BIGINT NOT NULL DEFAULT 0,
  "compliance_subject_id_field" TEXT,
  "compliance_enforce_consent" BIGINT NOT NULL DEFAULT 1,
  "vision_loop_enabled" BIGINT NOT NULL DEFAULT 0,
  "reasoning_enabled" BIGINT NOT NULL DEFAULT 0,
  "reasoning_effort" TEXT,
  "reasoning_budget_tokens" BIGINT NOT NULL DEFAULT 0,
  PRIMARY KEY ("chat_id"),
  FOREIGN KEY ("chat_id") REFERENCES "chats" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "user_runs" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "status" TEXT NOT NULL DEFAULT 'pending',
  "surface" TEXT,
  "metadata" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "coedit_docs" (
  "id" TEXT,
  "run_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "owner_id" TEXT NOT NULL,
  "title" TEXT,
  "snapshot_json" TEXT NOT NULL DEFAULT '{"nodes":[]}',
  "state_vector_json" TEXT NOT NULL DEFAULT '{}',
  "agent_written" BIGINT NOT NULL DEFAULT 0,
  "created_at" BIGINT NOT NULL,
  "updated_at" BIGINT NOT NULL,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("run_id") REFERENCES "user_runs" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "coedit_ops" (
  "id" TEXT,
  "doc_id" TEXT NOT NULL,
  "op_site" TEXT NOT NULL,
  "op_counter" BIGINT NOT NULL,
  "op_json" TEXT NOT NULL,
  "created_at" BIGINT NOT NULL,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("doc_id") REFERENCES "coedit_docs" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "collaboration_config" (
  "id" TEXT DEFAULT 'global',
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "presence_heartbeat_ms" BIGINT NOT NULL DEFAULT 15000,
  "presence_ttl_ms" BIGINT NOT NULL DEFAULT 30000,
  "presence_sweep_ms" BIGINT NOT NULL DEFAULT 10000,
  "max_participants_per_run" BIGINT NOT NULL DEFAULT 50,
  "show_agent_presence" BIGINT NOT NULL DEFAULT 1,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "notify_relay_sweep_ms" BIGINT NOT NULL DEFAULT 5000,
  "notify_max_attempts" BIGINT NOT NULL DEFAULT 6,
  "notify_lease_ms" BIGINT NOT NULL DEFAULT 30000,
  "notify_base_backoff_ms" BIGINT NOT NULL DEFAULT 2000,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "collaboration_sessions" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "session_type" TEXT NOT NULL DEFAULT 'team',
  "max_participants" BIGINT NOT NULL DEFAULT 10,
  "presence_ttl_ms" BIGINT NOT NULL DEFAULT 30000,
  "auto_close_idle_ms" BIGINT,
  "handoff_enabled" BIGINT NOT NULL DEFAULT 1,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "compliance_rules" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "rule_type" TEXT NOT NULL DEFAULT 'retention',
  "target_resource" TEXT NOT NULL DEFAULT '*',
  "retention_days" BIGINT,
  "region" TEXT,
  "consent_purpose" TEXT,
  "action" TEXT NOT NULL DEFAULT 'notify',
  "config" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "cost_policies" (
  "id" TEXT,
  "key" TEXT NOT NULL,
  "tier" TEXT NOT NULL DEFAULT 'balanced',
  "levers_json" TEXT,
  "description" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "realm" TEXT NOT NULL DEFAULT 'global',
  "owner_tenant_id" TEXT,
  "logical_key" TEXT,
  "origin_id" TEXT,
  "origin_hash" TEXT,
  "content_hash" TEXT NOT NULL DEFAULT '',
  "track_mode" TEXT NOT NULL DEFAULT 'pin',
  "share_mode" TEXT NOT NULL DEFAULT 'private',
  "deprecated_at" TEXT,
  "deprecation_note" TEXT,
  "superseded_by_id" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "email_verifications" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "token_hash" TEXT NOT NULL,
  "expires_at" TEXT NOT NULL,
  "used_at" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "encryption_audit" (
  "id" TEXT,
  "tenant_id" TEXT NOT NULL,
  "event_kind" TEXT NOT NULL,
  "actor" TEXT,
  "details" TEXT,
  "created_at" BIGINT NOT NULL,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "encryption_rewrite_progress" (
  "id" TEXT,
  "tenant_id" TEXT NOT NULL,
  "table_name" TEXT NOT NULL,
  "column_name" TEXT NOT NULL,
  "from_epoch" BIGINT NOT NULL,
  "to_epoch" BIGINT NOT NULL,
  "last_row_id" TEXT,
  "rows_rewritten" BIGINT NOT NULL DEFAULT 0,
  "status" TEXT NOT NULL DEFAULT 'pending',
  "created_at" BIGINT NOT NULL,
  "updated_at" BIGINT NOT NULL,
  "completed_at" BIGINT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "endpoint_health" (
  "endpoint" TEXT,
  "circuit_state" TEXT,
  "consecutive_failures" BIGINT NOT NULL DEFAULT 0,
  "last_signal_at" TEXT,
  "last_429_at" TEXT,
  "last_retry_after_ms" BIGINT,
  "last_circuit_opened_at" TEXT,
  "last_circuit_closed_at" TEXT,
  "total_success" BIGINT NOT NULL DEFAULT 0,
  "total_failed" BIGINT NOT NULL DEFAULT 0,
  "total_rate_limited" BIGINT NOT NULL DEFAULT 0,
  "total_retries" BIGINT NOT NULL DEFAULT 0,
  "total_shed" BIGINT NOT NULL DEFAULT 0,
  "total_circuit_opens" BIGINT NOT NULL DEFAULT 0,
  "avg_latency_ms" DOUBLE PRECISION,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("endpoint")
);

CREATE TABLE IF NOT EXISTS "enterprise_connectors" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "connector_type" TEXT NOT NULL,
  "base_url" TEXT,
  "auth_type" TEXT,
  "auth_config" TEXT,
  "access_token" TEXT,
  "refresh_token" TEXT,
  "token_expires_at" TEXT,
  "oauth_state" TEXT,
  "status" TEXT NOT NULL DEFAULT 'disconnected',
  "options" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "credentials_encrypted" BIGINT NOT NULL DEFAULT 0,
  "access_token_enc" TEXT,
  "refresh_token_enc" TEXT,
  "auth_config_enc" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "entity_memory" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "chat_id" TEXT,
  "tenant_id" TEXT,
  "entity_name" TEXT NOT NULL,
  "entity_type" TEXT NOT NULL DEFAULT 'general',
  "facts" TEXT NOT NULL DEFAULT '{}',
  "confidence" DOUBLE PRECISION NOT NULL DEFAULT 0.5,
  "source" TEXT NOT NULL DEFAULT 'regex',
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("chat_id") REFERENCES "chats" ("id") ON DELETE SET NULL,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "episodic_memory" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "chat_id" TEXT,
  "tenant_id" TEXT,
  "message_role" TEXT NOT NULL DEFAULT 'user',
  "content" TEXT NOT NULL,
  "importance" DOUBLE PRECISION NOT NULL DEFAULT 0.5,
  "tags" TEXT,
  "consolidated" BIGINT NOT NULL DEFAULT 0,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("chat_id") REFERENCES "chats" ("id") ON DELETE SET NULL,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "eval_results" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "chat_id" TEXT,
  "eval_name" TEXT NOT NULL,
  "score" DOUBLE PRECISION NOT NULL,
  "passed" BIGINT NOT NULL DEFAULT 0,
  "failed" BIGINT NOT NULL DEFAULT 0,
  "total" BIGINT NOT NULL DEFAULT 0,
  "system_prompt" TEXT,
  "timezone" TEXT,
  "enabled_tools" TEXT,
  "redaction_enabled" BIGINT NOT NULL DEFAULT 0,
  "redaction_patterns" TEXT,
  "workers" TEXT,
  "settings_snapshot_at" TEXT,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("user_id") REFERENCES "users" ("id")
);

CREATE TABLE IF NOT EXISTS "extraction_pipelines" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "stages" TEXT NOT NULL,
  "input_mime_types" TEXT,
  "max_input_size_bytes" BIGINT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "graph_configs" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "graph_type" TEXT NOT NULL DEFAULT 'entity',
  "max_depth" BIGINT NOT NULL DEFAULT 3,
  "entity_types" TEXT,
  "relationship_types" TEXT,
  "auto_link" BIGINT NOT NULL DEFAULT 1,
  "scoring_weights" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "guardrail_evals" (
  "id" TEXT,
  "chat_id" TEXT,
  "message_id" TEXT,
  "stage" TEXT NOT NULL,
  "input_preview" TEXT,
  "results" TEXT NOT NULL DEFAULT '[]',
  "overall_decision" TEXT NOT NULL DEFAULT 'allow',
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "escalation" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "guardrail_revisions" (
  "id" TEXT,
  "guardrail_id" TEXT NOT NULL,
  "version" BIGINT NOT NULL,
  "snapshot" TEXT NOT NULL,
  "before" TEXT,
  "actor" TEXT NOT NULL DEFAULT 'system',
  "reason" TEXT NOT NULL DEFAULT '',
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "guardrails" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "type" TEXT NOT NULL,
  "stage" TEXT NOT NULL DEFAULT 'pre',
  "config" TEXT,
  "priority" BIGINT NOT NULL DEFAULT 0,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "trigger_conditions" TEXT,
  "trigger_description" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "judge_model" TEXT,
  "compliance_framework" TEXT,
  "realm" TEXT NOT NULL DEFAULT 'global',
  "owner_tenant_id" TEXT,
  "logical_key" TEXT,
  "origin_id" TEXT,
  "origin_hash" TEXT,
  "content_hash" TEXT NOT NULL DEFAULT '',
  "track_mode" TEXT NOT NULL DEFAULT 'pin',
  "share_mode" TEXT NOT NULL DEFAULT 'private',
  "deprecated_at" TEXT,
  "deprecation_note" TEXT,
  "superseded_by_id" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "session_handoffs" (
  "id" TEXT,
  "run_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "scope" TEXT NOT NULL,
  "from_actor_type" TEXT NOT NULL,
  "from_actor_id" TEXT NOT NULL,
  "to_actor_type" TEXT NOT NULL,
  "to_actor_id" TEXT NOT NULL,
  "state" TEXT NOT NULL DEFAULT 'requested',
  "reason" TEXT NOT NULL,
  "briefing_json" TEXT,
  "rejection_reason" TEXT,
  "hand_back_briefing_json" TEXT,
  "depth" BIGINT NOT NULL DEFAULT 0,
  "parent_handoff_id" TEXT,
  "reference_task_ids_json" TEXT NOT NULL DEFAULT '[]',
  "created_at" BIGINT NOT NULL,
  "updated_at" BIGINT NOT NULL,
  "resolved_at" BIGINT,
  "expires_at" BIGINT,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("run_id") REFERENCES "user_runs" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "handoff_events" (
  "id" TEXT,
  "handoff_id" TEXT NOT NULL,
  "at" BIGINT NOT NULL,
  "actor_id" TEXT NOT NULL,
  "from_state" TEXT,
  "to_state" TEXT NOT NULL,
  "note" TEXT,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("handoff_id") REFERENCES "session_handoffs" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "hitl_interrupt_requests" (
  "id" TEXT,
  "chat_id" TEXT NOT NULL,
  "message_id" TEXT,
  "agent_name" TEXT NOT NULL,
  "agent_step" BIGINT NOT NULL,
  "tool_name" TEXT NOT NULL,
  "tool_args_json" TEXT NOT NULL DEFAULT '{}',
  "interrupt_type" TEXT NOT NULL DEFAULT 'tool_approval',
  "reason" TEXT NOT NULL DEFAULT '',
  "status" TEXT NOT NULL DEFAULT 'pending',
  "decision_action" TEXT,
  "modified_args_json" TEXT,
  "feedback" TEXT,
  "assignee" TEXT,
  "decided_by" TEXT,
  "decided_at" TEXT,
  "expires_at" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD"T"HH24:MI:SS.MS"Z"'),
  "run_id" TEXT,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("chat_id") REFERENCES "chats" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "http_endpoints" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "url" TEXT NOT NULL,
  "method" TEXT NOT NULL DEFAULT 'GET',
  "auth_type" TEXT,
  "auth_config" TEXT,
  "headers" TEXT,
  "body_template" TEXT,
  "response_transform" TEXT,
  "retry_count" BIGINT NOT NULL DEFAULT 2,
  "rate_limit_rpm" BIGINT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "human_task_policies" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "trigger" TEXT NOT NULL,
  "task_type" TEXT NOT NULL DEFAULT 'approval',
  "default_priority" TEXT NOT NULL DEFAULT 'normal',
  "sla_hours" DOUBLE PRECISION,
  "auto_escalate_after_hours" DOUBLE PRECISION,
  "assignment_strategy" TEXT NOT NULL DEFAULT 'round-robin',
  "assign_to" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "hv_budget_envelope" (
  "id" TEXT,
  "tenant_id" TEXT NOT NULL,
  "name" TEXT NOT NULL,
  "max_llm_cents" BIGINT NOT NULL,
  "max_sandbox_cents" BIGINT NOT NULL,
  "max_wall_seconds" BIGINT NOT NULL,
  "max_rounds" BIGINT NOT NULL,
  "diminishing_returns_epsilon" DOUBLE PRECISION NOT NULL,
  "created_at" TEXT NOT NULL,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "hv_hypothesis" (
  "id" TEXT,
  "tenant_id" TEXT NOT NULL,
  "submitted_by" TEXT NOT NULL,
  "title" TEXT NOT NULL,
  "statement" TEXT NOT NULL,
  "domain_tags" TEXT NOT NULL,
  "status" TEXT NOT NULL DEFAULT 'queued',
  "budget_envelope_id" TEXT NOT NULL,
  "workflow_run_id" TEXT,
  "trace_id" TEXT,
  "contract_id" TEXT,
  "created_at" TEXT NOT NULL,
  "updated_at" TEXT NOT NULL,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("budget_envelope_id") REFERENCES "hv_budget_envelope" ("id")
);

CREATE TABLE IF NOT EXISTS "hv_agent_turn" (
  "id" TEXT,
  "hypothesis_id" TEXT NOT NULL,
  "round_index" BIGINT NOT NULL DEFAULT 0,
  "from_agent" TEXT NOT NULL,
  "to_agent" TEXT,
  "message" TEXT NOT NULL,
  "cites_evidence_ids" TEXT NOT NULL DEFAULT '[]',
  "dissent" BIGINT NOT NULL DEFAULT 0,
  "created_at" TEXT NOT NULL,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("hypothesis_id") REFERENCES "hv_hypothesis" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "hv_evidence_event" (
  "id" TEXT,
  "hypothesis_id" TEXT NOT NULL,
  "step_id" TEXT NOT NULL,
  "agent_id" TEXT NOT NULL,
  "evidence_id" TEXT NOT NULL,
  "kind" TEXT NOT NULL,
  "summary" TEXT NOT NULL,
  "source_type" TEXT NOT NULL,
  "tool_key" TEXT,
  "reproducibility_hash" TEXT,
  "created_at" TEXT NOT NULL,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("hypothesis_id") REFERENCES "hv_hypothesis" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "hv_sub_claim" (
  "id" TEXT,
  "tenant_id" TEXT NOT NULL,
  "hypothesis_id" TEXT NOT NULL,
  "parent_sub_claim_id" TEXT,
  "statement" TEXT NOT NULL,
  "claim_type" TEXT NOT NULL,
  "testability_score" DOUBLE PRECISION NOT NULL,
  "created_at" TEXT NOT NULL,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("parent_sub_claim_id") REFERENCES "hv_sub_claim" ("id"),
  FOREIGN KEY ("hypothesis_id") REFERENCES "hv_hypothesis" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "hv_verdict" (
  "id" TEXT,
  "tenant_id" TEXT NOT NULL,
  "hypothesis_id" TEXT NOT NULL,
  "verdict" TEXT NOT NULL,
  "confidence_lo" DOUBLE PRECISION NOT NULL,
  "confidence_hi" DOUBLE PRECISION NOT NULL,
  "key_evidence_ids" TEXT NOT NULL,
  "falsifiers" TEXT NOT NULL,
  "limitations" TEXT NOT NULL,
  "contract_id" TEXT NOT NULL,
  "replay_trace_id" TEXT NOT NULL,
  "emitted_by" TEXT NOT NULL DEFAULT 'supervisor',
  "created_at" TEXT NOT NULL,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("hypothesis_id") REFERENCES "hv_hypothesis" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "idempotency_records" (
  "id" TEXT,
  "key" TEXT NOT NULL,
  "result_json" TEXT NOT NULL,
  "expires_at" TEXT NOT NULL,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "identity_rules" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "resource" TEXT NOT NULL,
  "action" TEXT NOT NULL DEFAULT '*',
  "roles" TEXT,
  "scopes" TEXT,
  "result" TEXT NOT NULL DEFAULT 'allow',
  "priority" BIGINT NOT NULL DEFAULT 0,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "kaggle_approaches" (
  "id" TEXT,
  "tenant_id" TEXT,
  "competition_ref" TEXT NOT NULL,
  "summary" TEXT NOT NULL,
  "expected_metric" TEXT,
  "model" TEXT,
  "source_kernel_refs" TEXT,
  "embedding" BYTEA,
  "status" TEXT NOT NULL DEFAULT 'draft',
  "created_by" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "ensemble_member_of" TEXT,
  "blend_weights" TEXT,
  "expected_metric_value" DOUBLE PRECISION,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "kaggle_competition_rubric" (
  "id" TEXT,
  "tenant_id" TEXT,
  "competition_ref" TEXT NOT NULL,
  "metric_name" TEXT,
  "metric_direction" TEXT,
  "baseline_score" DOUBLE PRECISION,
  "target_score" DOUBLE PRECISION,
  "expected_row_count" BIGINT,
  "id_column" TEXT,
  "id_range_min" BIGINT,
  "id_range_max" BIGINT,
  "target_column" TEXT,
  "target_type" TEXT,
  "expected_distribution_json" TEXT,
  "sample_submission_sha256" TEXT,
  "inference_source" TEXT,
  "auto_generated" BIGINT NOT NULL DEFAULT 1,
  "inferred_at" TEXT,
  "notes" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "kaggle_competitions_tracked" (
  "id" TEXT,
  "tenant_id" TEXT,
  "competition_ref" TEXT NOT NULL,
  "title" TEXT,
  "category" TEXT,
  "deadline" TEXT,
  "reward" TEXT,
  "url" TEXT,
  "status" TEXT NOT NULL DEFAULT 'watching',
  "notes" TEXT,
  "last_synced_at" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "kaggle_discussion_posts" (
  "id" TEXT,
  "tenant_id" TEXT,
  "competition_ref" TEXT NOT NULL,
  "topic_id" TEXT NOT NULL,
  "parent_topic_id" TEXT,
  "title" TEXT,
  "body_preview" TEXT,
  "url" TEXT,
  "status" TEXT NOT NULL DEFAULT 'posted',
  "contract_id" TEXT,
  "replay_trace_id" TEXT,
  "posted_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "kaggle_discussion_settings" (
  "id" TEXT,
  "tenant_id" TEXT NOT NULL,
  "discussion_enabled" BIGINT NOT NULL DEFAULT 0,
  "notes" TEXT,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "kaggle_leaderboard_scores" (
  "id" TEXT,
  "run_id" TEXT,
  "competition_ref" TEXT NOT NULL,
  "submission_id" TEXT,
  "public_score" DOUBLE PRECISION,
  "private_score" DOUBLE PRECISION,
  "cv_lb_delta" DOUBLE PRECISION,
  "percentile_estimate" DOUBLE PRECISION,
  "rank_estimate" BIGINT,
  "leaderboard_size" BIGINT,
  "raw_status" TEXT,
  "observed_at" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "kaggle_live_mesh_index" (
  "mesh_id" TEXT,
  "tenant_id" TEXT NOT NULL,
  "kaggle_username" TEXT NOT NULL,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("mesh_id")
);

CREATE TABLE IF NOT EXISTS "kaggle_role_capabilities" (
  "role" TEXT,
  "capabilities" TEXT NOT NULL,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_by" TEXT,
  PRIMARY KEY ("role")
);

CREATE TABLE IF NOT EXISTS "kaggle_runs" (
  "id" TEXT,
  "tenant_id" TEXT,
  "competition_ref" TEXT NOT NULL,
  "approach_id" TEXT,
  "contract_id" TEXT,
  "replay_trace_id" TEXT,
  "mesh_id" TEXT,
  "agent_id" TEXT,
  "kernel_ref" TEXT,
  "submission_id" TEXT,
  "public_score" DOUBLE PRECISION,
  "validator_report" TEXT,
  "status" TEXT NOT NULL DEFAULT 'queued',
  "started_at" TEXT,
  "completed_at" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "cv_score" DOUBLE PRECISION,
  "cv_metric" TEXT,
  "oof_path" TEXT,
  "is_ensemble" BIGINT NOT NULL DEFAULT 0,
  "ensemble_member_run_ids" TEXT,
  "private_score" DOUBLE PRECISION,
  "is_final_pick" BIGINT NOT NULL DEFAULT 0,
  "finalized_at" TEXT,
  "cv_lb_gap" DOUBLE PRECISION,
  "kernel_outputs" TEXT,
  "search_results" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "kaggle_run_artifacts" (
  "id" TEXT,
  "run_id" TEXT NOT NULL,
  "contract_id" TEXT NOT NULL,
  "replay_trace_id" TEXT NOT NULL,
  "contract_report_json" TEXT NOT NULL,
  "replay_run_log_json" TEXT NOT NULL,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("run_id") REFERENCES "kaggle_runs" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "kaggle_validation_results" (
  "id" TEXT,
  "run_id" TEXT NOT NULL,
  "competition_ref" TEXT NOT NULL,
  "rubric_id" TEXT,
  "kernel_ref" TEXT,
  "schema_check_passed" BIGINT,
  "distribution_check_passed" BIGINT,
  "baseline_check_passed" BIGINT,
  "cv_score" DOUBLE PRECISION,
  "cv_std" DOUBLE PRECISION,
  "cv_metric" TEXT,
  "n_folds" BIGINT,
  "predicted_distribution_json" TEXT,
  "violations_json" TEXT,
  "verdict" TEXT,
  "summary" TEXT,
  "validated_at" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "kgl_competition_run" (
  "id" TEXT,
  "tenant_id" TEXT NOT NULL,
  "submitted_by" TEXT NOT NULL,
  "competition_ref" TEXT NOT NULL,
  "title" TEXT,
  "objective" TEXT,
  "mesh_id" TEXT,
  "status" TEXT NOT NULL DEFAULT 'queued',
  "step_count" BIGINT NOT NULL DEFAULT 0,
  "event_count" BIGINT NOT NULL DEFAULT 0,
  "summary" TEXT,
  "started_at" TEXT,
  "completed_at" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "kgl_run_step" (
  "id" TEXT,
  "run_id" TEXT NOT NULL,
  "step_index" BIGINT NOT NULL,
  "role" TEXT NOT NULL,
  "title" TEXT NOT NULL,
  "description" TEXT,
  "agent_id" TEXT,
  "status" TEXT NOT NULL DEFAULT 'pending',
  "started_at" TEXT,
  "completed_at" TEXT,
  "summary" TEXT,
  "input_preview" TEXT,
  "output_preview" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("run_id") REFERENCES "kgl_competition_run" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "kgl_run_event" (
  "id" TEXT,
  "run_id" TEXT NOT NULL,
  "step_id" TEXT,
  "kind" TEXT NOT NULL,
  "agent_id" TEXT,
  "tool_key" TEXT,
  "summary" TEXT NOT NULL,
  "payload_json" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("step_id") REFERENCES "kgl_run_step" ("id") ON DELETE SET NULL,
  FOREIGN KEY ("run_id") REFERENCES "kgl_competition_run" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "la_entities" (
  "entity_type" TEXT NOT NULL,
  "id" TEXT NOT NULL,
  "payload_json" TEXT NOT NULL,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("entity_type", "id")
);

CREATE TABLE IF NOT EXISTS "live_mesh_definitions" (
  "id" TEXT,
  "mesh_key" TEXT NOT NULL,
  "name" TEXT NOT NULL,
  "charter_prose" TEXT NOT NULL,
  "dual_control_required_for" TEXT NOT NULL DEFAULT '[]',
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "description" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "domain" TEXT,
  "bridge_topics_default" TEXT,
  "bridge_rate_limit_default" BIGINT,
  "provisioner_config_json" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "live_agent_definitions" (
  "id" TEXT,
  "mesh_def_id" TEXT NOT NULL,
  "role_key" TEXT NOT NULL,
  "name" TEXT NOT NULL,
  "role_label" TEXT NOT NULL,
  "persona" TEXT NOT NULL,
  "objectives" TEXT NOT NULL,
  "success_indicators" TEXT NOT NULL,
  "ordering" BIGINT NOT NULL DEFAULT 0,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "default_handler_kind" TEXT,
  "default_handler_config_json" TEXT,
  "default_tool_catalog_keys" TEXT,
  "default_attention_policy_key" TEXT,
  "model_capability_json" TEXT,
  "model_routing_policy_key" TEXT,
  "model_pinned_id" TEXT,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("mesh_def_id") REFERENCES "live_mesh_definitions" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "live_meshes" (
  "id" TEXT,
  "tenant_id" TEXT,
  "mesh_def_id" TEXT NOT NULL,
  "name" TEXT NOT NULL,
  "status" TEXT NOT NULL DEFAULT 'ACTIVE',
  "domain" TEXT,
  "dual_control_required_for" TEXT NOT NULL DEFAULT '[]',
  "owner_human_id" TEXT,
  "mcp_server_ref" TEXT,
  "account_id" TEXT,
  "context_json" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("mesh_def_id") REFERENCES "live_mesh_definitions" ("id") ON DELETE RESTRICT
);

CREATE TABLE IF NOT EXISTS "live_agents" (
  "id" TEXT,
  "mesh_id" TEXT NOT NULL,
  "agent_def_id" TEXT,
  "role_key" TEXT NOT NULL,
  "name" TEXT NOT NULL,
  "role_label" TEXT NOT NULL,
  "persona" TEXT NOT NULL,
  "objectives" TEXT NOT NULL,
  "success_indicators" TEXT NOT NULL,
  "attention_policy_key" TEXT,
  "contract_version_id" TEXT,
  "status" TEXT NOT NULL DEFAULT 'ACTIVE',
  "ordering" BIGINT NOT NULL DEFAULT 0,
  "archived_at" TEXT,
  "model_capability_json" TEXT,
  "model_routing_policy_key" TEXT,
  "model_pinned_id" TEXT,
  "prepare_config_json" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("agent_def_id") REFERENCES "live_agent_definitions" ("id") ON DELETE SET NULL,
  FOREIGN KEY ("mesh_id") REFERENCES "live_meshes" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "live_agent_handler_bindings" (
  "id" TEXT,
  "agent_id" TEXT NOT NULL,
  "handler_kind" TEXT NOT NULL,
  "config_json" TEXT NOT NULL DEFAULT '{}',
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("agent_id") REFERENCES "live_agents" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "tool_catalog" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "category" TEXT,
  "risk_level" TEXT NOT NULL DEFAULT 'read-only',
  "requires_approval" BIGINT NOT NULL DEFAULT 0,
  "max_execution_ms" BIGINT,
  "rate_limit_per_min" BIGINT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "tool_key" TEXT,
  "version" TEXT NOT NULL DEFAULT '1.0',
  "side_effects" BIGINT NOT NULL DEFAULT 0,
  "tags" TEXT,
  "source" TEXT NOT NULL DEFAULT 'builtin',
  "credential_id" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "config" TEXT,
  "allocation_class" TEXT,
  "domain_tags" TEXT,
  "requires" TEXT,
  "display_name" TEXT,
  "requires_capabilities" TEXT NOT NULL DEFAULT '[]',
  "agentic_scope" TEXT NOT NULL DEFAULT 'system',
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "live_agent_tool_bindings" (
  "id" TEXT,
  "agent_id" TEXT NOT NULL,
  "tool_catalog_id" TEXT,
  "mcp_server_url" TEXT,
  "capability_keys" TEXT NOT NULL DEFAULT '[]',
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("tool_catalog_id") REFERENCES "tool_catalog" ("id") ON DELETE CASCADE,
  FOREIGN KEY ("agent_id") REFERENCES "live_agents" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "live_artifact_configs" (
  "id" TEXT,
  "artifact_id" TEXT NOT NULL,
  "mcp_server_key" TEXT,
  "refresh_tool" TEXT,
  "refresh_args" TEXT,
  "refresh_interval_seconds" BIGINT NOT NULL DEFAULT 0,
  "cache_ttl_seconds" BIGINT NOT NULL DEFAULT 30,
  "last_refreshed_at" TEXT,
  "refresh_count" BIGINT NOT NULL DEFAULT 0,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD"T"HH24:MI:SS.MS"Z"'),
  "updated_at" TEXT,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("artifact_id") REFERENCES "artifacts" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "live_attention_policies" (
  "id" TEXT,
  "key" TEXT NOT NULL,
  "kind" TEXT NOT NULL,
  "description" TEXT NOT NULL,
  "config_json" TEXT NOT NULL DEFAULT '{}',
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "realm" TEXT NOT NULL DEFAULT 'global',
  "owner_tenant_id" TEXT,
  "logical_key" TEXT,
  "origin_id" TEXT,
  "origin_hash" TEXT,
  "content_hash" TEXT NOT NULL DEFAULT '',
  "track_mode" TEXT NOT NULL DEFAULT 'pin',
  "share_mode" TEXT NOT NULL DEFAULT 'private',
  "deprecated_at" TEXT,
  "deprecation_note" TEXT,
  "superseded_by_id" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "live_handler_kinds" (
  "id" TEXT,
  "kind" TEXT NOT NULL,
  "description" TEXT NOT NULL,
  "config_schema_json" TEXT NOT NULL DEFAULT '{}',
  "source" TEXT NOT NULL DEFAULT 'builtin',
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "realm" TEXT NOT NULL DEFAULT 'global',
  "owner_tenant_id" TEXT,
  "logical_key" TEXT,
  "origin_id" TEXT,
  "origin_hash" TEXT,
  "content_hash" TEXT NOT NULL DEFAULT '',
  "track_mode" TEXT NOT NULL DEFAULT 'pin',
  "share_mode" TEXT NOT NULL DEFAULT 'private',
  "deprecated_at" TEXT,
  "deprecation_note" TEXT,
  "superseded_by_id" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "live_mesh_delegation_edges" (
  "id" TEXT,
  "mesh_def_id" TEXT NOT NULL,
  "from_role_key" TEXT NOT NULL,
  "to_role_key" TEXT NOT NULL,
  "relationship" TEXT NOT NULL,
  "prose" TEXT NOT NULL,
  "ordering" BIGINT NOT NULL DEFAULT 0,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("mesh_def_id") REFERENCES "live_mesh_definitions" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "live_runs" (
  "id" TEXT,
  "mesh_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "run_key" TEXT NOT NULL,
  "label" TEXT,
  "status" TEXT NOT NULL DEFAULT 'RUNNING',
  "started_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "completed_at" TEXT,
  "summary" TEXT,
  "context_json" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "stop_requested" BIGINT NOT NULL DEFAULT 0,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("mesh_id") REFERENCES "live_meshes" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "live_run_steps" (
  "id" TEXT,
  "run_id" TEXT NOT NULL,
  "mesh_id" TEXT NOT NULL,
  "agent_id" TEXT,
  "role_key" TEXT NOT NULL,
  "status" TEXT NOT NULL DEFAULT 'PENDING',
  "started_at" TEXT,
  "completed_at" TEXT,
  "summary" TEXT,
  "payload_json" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("agent_id") REFERENCES "live_agents" ("id") ON DELETE SET NULL,
  FOREIGN KEY ("mesh_id") REFERENCES "live_meshes" ("id") ON DELETE CASCADE,
  FOREIGN KEY ("run_id") REFERENCES "live_runs" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "live_run_events" (
  "id" TEXT,
  "run_id" TEXT NOT NULL,
  "step_id" TEXT,
  "kind" TEXT NOT NULL,
  "agent_id" TEXT,
  "tool_key" TEXT,
  "summary" TEXT,
  "payload_json" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("agent_id") REFERENCES "live_agents" ("id") ON DELETE SET NULL,
  FOREIGN KEY ("step_id") REFERENCES "live_run_steps" ("id") ON DELETE SET NULL,
  FOREIGN KEY ("run_id") REFERENCES "live_runs" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "mcp_gateway_clients" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "token_hash" TEXT NOT NULL,
  "allowed_classes" TEXT,
  "audit_chat_id" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "last_used_at" TEXT,
  "revoked_at" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "rate_limit_per_minute" BIGINT,
  "expires_at" TEXT,
  "rotated_at" TEXT,
  "tenant_id" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "mcp_gateway_rate_buckets" (
  "id" TEXT,
  "tenant_id" TEXT NOT NULL DEFAULT '',
  "client_id" TEXT NOT NULL,
  "window_start" TEXT NOT NULL,
  "count" BIGINT NOT NULL DEFAULT 0,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "mcp_gateway_request_log" (
  "id" TEXT,
  "client_id" TEXT,
  "client_name" TEXT,
  "method" TEXT,
  "tool_name" TEXT,
  "outcome" TEXT NOT NULL,
  "status_code" BIGINT NOT NULL,
  "duration_ms" BIGINT,
  "error_message" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "retention_tier" TEXT NOT NULL DEFAULT 'operational',
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "memory_extraction_events" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "chat_id" TEXT,
  "tenant_id" TEXT,
  "self_disclosure" BIGINT NOT NULL DEFAULT 0,
  "regex_entities_count" BIGINT NOT NULL DEFAULT 0,
  "llm_entities_count" BIGINT NOT NULL DEFAULT 0,
  "merged_entities_count" BIGINT NOT NULL DEFAULT 0,
  "events" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("chat_id") REFERENCES "chats" ("id") ON DELETE SET NULL,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "memory_extraction_rules" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "rule_type" TEXT NOT NULL,
  "entity_type" TEXT,
  "pattern" TEXT NOT NULL,
  "flags" TEXT,
  "facts_template" TEXT,
  "priority" BIGINT NOT NULL DEFAULT 0,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "memory_governance" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "memory_types" TEXT,
  "tenant_id" TEXT,
  "block_patterns" TEXT,
  "redact_patterns" TEXT,
  "max_age" TEXT,
  "max_entries" BIGINT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "memory_settings" (
  "id" TEXT,
  "tenant_id" TEXT,
  "enable_semantic" BIGINT NOT NULL DEFAULT 1,
  "enable_entity" BIGINT NOT NULL DEFAULT 1,
  "enable_episodic" BIGINT NOT NULL DEFAULT 1,
  "enable_procedural" BIGINT NOT NULL DEFAULT 1,
  "enable_working" BIGINT NOT NULL DEFAULT 1,
  "auto_extract_on_turn" BIGINT NOT NULL DEFAULT 1,
  "consolidation_enabled" BIGINT NOT NULL DEFAULT 1,
  "consolidation_interval_min" BIGINT NOT NULL DEFAULT 60,
  "max_episodic_per_user" BIGINT NOT NULL DEFAULT 200,
  "max_semantic_per_user" BIGINT NOT NULL DEFAULT 500,
  "max_entity_per_user" BIGINT NOT NULL DEFAULT 100,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "mesh_contracts" (
  "id" TEXT,
  "kind" TEXT NOT NULL,
  "body_json" TEXT NOT NULL DEFAULT '{}',
  "evidence_json" TEXT,
  "mesh_id" TEXT,
  "source_workflow_definition_id" TEXT,
  "source_workflow_run_id" TEXT,
  "source_agent_id" TEXT,
  "metadata" TEXT,
  "emitted_at" TEXT NOT NULL,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "message_citations" (
  "id" TEXT,
  "message_id" TEXT NOT NULL,
  "chat_id" TEXT,
  "user_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "n" BIGINT NOT NULL,
  "source_id" TEXT NOT NULL,
  "source_kind" TEXT NOT NULL,
  "source_title" TEXT NOT NULL,
  "quote" TEXT NOT NULL,
  "char_start" BIGINT NOT NULL,
  "char_end" BIGINT NOT NULL,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "message_feedback" (
  "id" TEXT,
  "message_id" TEXT NOT NULL,
  "chat_id" TEXT,
  "user_id" TEXT,
  "signal" TEXT NOT NULL,
  "comment" TEXT,
  "model_id" TEXT,
  "provider" TEXT,
  "task_key" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "categories" TEXT,
  "tenant_id" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "message_variants" (
  "id" TEXT,
  "group_id" TEXT NOT NULL,
  "chat_id" TEXT NOT NULL,
  "user_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "variant_index" BIGINT NOT NULL,
  "content" TEXT NOT NULL,
  "model" TEXT,
  "provider" TEXT,
  "reason" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "messages" (
  "id" TEXT,
  "chat_id" TEXT NOT NULL,
  "role" TEXT NOT NULL,
  "content" TEXT NOT NULL,
  "metadata" TEXT,
  "tokens_used" BIGINT NOT NULL DEFAULT 0,
  "cost" DOUBLE PRECISION NOT NULL DEFAULT 0,
  "latency_ms" BIGINT NOT NULL DEFAULT 0,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("chat_id") REFERENCES "chats" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "metrics" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "chat_id" TEXT,
  "type" TEXT NOT NULL,
  "provider" TEXT,
  "model" TEXT,
  "prompt_tokens" BIGINT NOT NULL DEFAULT 0,
  "completion_tokens" BIGINT NOT NULL DEFAULT 0,
  "total_tokens" BIGINT NOT NULL DEFAULT 0,
  "cost" DOUBLE PRECISION NOT NULL DEFAULT 0,
  "latency_ms" BIGINT NOT NULL DEFAULT 0,
  "metadata" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("user_id") REFERENCES "users" ("id")
);

CREATE TABLE IF NOT EXISTS "mined_skill_proposals" (
  "id" TEXT,
  "proposed_skill_id" TEXT NOT NULL,
  "name" TEXT NOT NULL,
  "description" TEXT NOT NULL DEFAULT '',
  "instructions" TEXT NOT NULL DEFAULT '',
  "tool_names" TEXT NOT NULL DEFAULT '[]',
  "pattern" TEXT NOT NULL DEFAULT '',
  "occurrences" BIGINT NOT NULL DEFAULT 0,
  "evidence" TEXT NOT NULL DEFAULT '{}',
  "safety" TEXT NOT NULL DEFAULT '{}',
  "status" TEXT NOT NULL DEFAULT 'pending',
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "reviewed_at" TEXT,
  "reviewed_by" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "mode_labels" (
  "id" TEXT,
  "surface_id" TEXT NOT NULL,
  "mode_key" TEXT NOT NULL,
  "label" TEXT NOT NULL,
  "description" TEXT,
  "icon" TEXT,
  "is_default" BIGINT NOT NULL DEFAULT 0,
  "sort_order" BIGINT NOT NULL DEFAULT 0,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "metadata" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "model_capability_scores" (
  "id" TEXT,
  "tenant_id" TEXT,
  "model_id" TEXT NOT NULL,
  "provider" TEXT NOT NULL,
  "task_key" TEXT NOT NULL,
  "quality_score" DOUBLE PRECISION NOT NULL,
  "supports_tools" BIGINT NOT NULL DEFAULT 1,
  "supports_streaming" BIGINT NOT NULL DEFAULT 1,
  "supports_thinking" BIGINT NOT NULL DEFAULT 0,
  "supports_json_mode" BIGINT NOT NULL DEFAULT 0,
  "supports_vision" BIGINT NOT NULL DEFAULT 0,
  "max_output_tokens" BIGINT,
  "benchmark_source" TEXT,
  "raw_benchmark_score" DOUBLE PRECISION,
  "is_active" BIGINT NOT NULL DEFAULT 1,
  "last_evaluated_at" TEXT,
  "production_signal_score" DOUBLE PRECISION,
  "signal_sample_count" BIGINT NOT NULL DEFAULT 0,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "supports_computer_use" BIGINT NOT NULL DEFAULT 0,
  "supports_long_context" BIGINT NOT NULL DEFAULT 0,
  "supports_realtime_audio" BIGINT NOT NULL DEFAULT 0,
  "realm" TEXT NOT NULL DEFAULT 'global',
  "owner_tenant_id" TEXT,
  "logical_key" TEXT,
  "origin_id" TEXT,
  "origin_hash" TEXT,
  "content_hash" TEXT NOT NULL DEFAULT '',
  "track_mode" TEXT NOT NULL DEFAULT 'pin',
  "share_mode" TEXT NOT NULL DEFAULT 'private',
  "deprecated_at" TEXT,
  "deprecation_note" TEXT,
  "superseded_by_id" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "model_pricing" (
  "id" TEXT,
  "model_id" TEXT NOT NULL,
  "provider" TEXT NOT NULL,
  "display_name" TEXT,
  "input_cost_per_1m" DOUBLE PRECISION NOT NULL DEFAULT 0,
  "output_cost_per_1m" DOUBLE PRECISION NOT NULL DEFAULT 0,
  "quality_score" DOUBLE PRECISION NOT NULL DEFAULT 0.7,
  "source" TEXT NOT NULL DEFAULT 'manual',
  "last_synced_at" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "prompt_cache_enabled" BIGINT NOT NULL DEFAULT 1,
  "prompt_cache_min_tokens" BIGINT NOT NULL DEFAULT 1024,
  "prompt_cache_ttl" TEXT NOT NULL DEFAULT '5m',
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "output_modality" TEXT NOT NULL DEFAULT 'text',
  "context_window_k" BIGINT,
  "max_output_tokens_k" BIGINT,
  "realm" TEXT NOT NULL DEFAULT 'global',
  "owner_tenant_id" TEXT,
  "logical_key" TEXT,
  "origin_id" TEXT,
  "origin_hash" TEXT,
  "content_hash" TEXT NOT NULL DEFAULT '',
  "track_mode" TEXT NOT NULL DEFAULT 'pin',
  "share_mode" TEXT NOT NULL DEFAULT 'private',
  "deprecated_at" TEXT,
  "deprecation_note" TEXT,
  "superseded_by_id" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "note_action_modes" (
  "id" TEXT,
  "tenant_id" TEXT NOT NULL DEFAULT '',
  "action_key" TEXT NOT NULL,
  "mode" TEXT NOT NULL DEFAULT 'direct',
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "notes" (
  "id" TEXT,
  "owner_user_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "title" TEXT NOT NULL DEFAULT 'Untitled',
  "icon" TEXT,
  "cover" TEXT,
  "parent_note_id" TEXT,
  "sensitivity" TEXT NOT NULL DEFAULT 'normal',
  "doc_json" TEXT NOT NULL DEFAULT '{"type":"doc","content":[]}',
  "is_template" BIGINT NOT NULL DEFAULT 0,
  "template_key" TEXT,
  "favorite" BIGINT NOT NULL DEFAULT 0,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "page_theme" TEXT NOT NULL DEFAULT 'pro',
  "freeform_mode" BIGINT NOT NULL DEFAULT 0,
  "cover_image_artifact_id" TEXT,
  "archived_at" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "note_activity" (
  "id" TEXT,
  "note_id" TEXT NOT NULL,
  "user_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "action" TEXT NOT NULL,
  "actor" TEXT NOT NULL DEFAULT 'user',
  "summary" TEXT,
  "detail_json" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("note_id") REFERENCES "notes" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "note_coedit_docs" (
  "id" TEXT,
  "note_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "owner_id" TEXT NOT NULL,
  "snapshot_json" TEXT NOT NULL DEFAULT '{"elements":[],"attrs":[],"marks":[]}',
  "state_vector_json" TEXT NOT NULL DEFAULT '{}',
  "created_at" BIGINT NOT NULL,
  "updated_at" BIGINT NOT NULL,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("note_id") REFERENCES "notes" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "note_coedit_ops" (
  "id" TEXT,
  "doc_id" TEXT NOT NULL,
  "op_site" TEXT NOT NULL,
  "op_counter" BIGINT NOT NULL,
  "op_json" TEXT NOT NULL,
  "created_at" BIGINT NOT NULL,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("doc_id") REFERENCES "note_coedit_docs" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "note_comments" (
  "id" TEXT,
  "note_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "thread_id" TEXT NOT NULL,
  "parent_id" TEXT,
  "author_id" TEXT NOT NULL,
  "body" TEXT NOT NULL,
  "body_html" TEXT NOT NULL,
  "mentions_json" TEXT NOT NULL DEFAULT '[]',
  "anchor_block_id" TEXT NOT NULL DEFAULT '',
  "created_at" BIGINT NOT NULL,
  "updated_at" BIGINT NOT NULL,
  "edited_at" BIGINT,
  "deleted_at" BIGINT,
  "deleted_by" TEXT,
  "resolved_at" BIGINT,
  "resolved_by" TEXT,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("note_id") REFERENCES "notes" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "note_databases" (
  "id" TEXT,
  "owner_user_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "name" TEXT NOT NULL,
  "source" TEXT NOT NULL DEFAULT 'generic',
  "view_type" TEXT NOT NULL DEFAULT 'table',
  "filter_json" TEXT NOT NULL DEFAULT '{}',
  "sort_json" TEXT NOT NULL DEFAULT '[]',
  "columns_json" TEXT NOT NULL DEFAULT '[]',
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "note_db_rows" (
  "id" TEXT,
  "database_id" TEXT NOT NULL,
  "fields_json" TEXT NOT NULL DEFAULT '{}',
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "note_embeddings" (
  "note_id" TEXT,
  "user_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "dim" BIGINT NOT NULL,
  "embedding_json" TEXT NOT NULL,
  "content_hash" TEXT NOT NULL,
  "title" TEXT,
  "updated_at" BIGINT NOT NULL,
  PRIMARY KEY ("note_id"),
  FOREIGN KEY ("note_id") REFERENCES "notes" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "note_entities" (
  "id" TEXT,
  "note_id" TEXT NOT NULL,
  "user_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "name" TEXT NOT NULL,
  "name_key" TEXT NOT NULL,
  "type" TEXT NOT NULL DEFAULT 'other',
  "created_at" BIGINT NOT NULL,
  "canonical_key" TEXT,
  "canonical_name" TEXT,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("note_id") REFERENCES "notes" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "note_flashcards" (
  "id" TEXT,
  "note_id" TEXT NOT NULL,
  "owner_user_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "front" TEXT NOT NULL,
  "back" TEXT NOT NULL,
  "ease_factor" DOUBLE PRECISION NOT NULL DEFAULT 2.5,
  "interval_days" BIGINT NOT NULL DEFAULT 0,
  "repetitions" BIGINT NOT NULL DEFAULT 0,
  "due_at" BIGINT NOT NULL,
  "last_reviewed_at" BIGINT,
  "created_at" BIGINT NOT NULL,
  "stability" DOUBLE PRECISION,
  "difficulty" DOUBLE PRECISION,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("note_id") REFERENCES "notes" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "note_links" (
  "id" TEXT,
  "note_id" TEXT NOT NULL,
  "target_kind" TEXT NOT NULL,
  "target_id" TEXT NOT NULL,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "note_meetings" (
  "id" TEXT,
  "note_id" TEXT NOT NULL,
  "user_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "title" TEXT NOT NULL DEFAULT 'Meeting notes',
  "source" TEXT NOT NULL DEFAULT 'recording',
  "language" TEXT,
  "duration_sec" BIGINT NOT NULL DEFAULT 0,
  "segments_json" TEXT NOT NULL DEFAULT '[]',
  "summary" TEXT,
  "action_items_json" TEXT NOT NULL DEFAULT '[]',
  "decisions_json" TEXT NOT NULL DEFAULT '[]',
  "cited" BIGINT NOT NULL DEFAULT 0,
  "cite_total" BIGINT NOT NULL DEFAULT 0,
  "audio_retained" BIGINT NOT NULL DEFAULT 0,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "note_memory_state" (
  "note_id" TEXT,
  "user_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "content_hash" TEXT NOT NULL,
  "memory_ids_json" TEXT NOT NULL DEFAULT '[]',
  "memory_count" BIGINT NOT NULL DEFAULT 0,
  "last_extracted_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("note_id")
);

CREATE TABLE IF NOT EXISTS "note_relations" (
  "id" TEXT,
  "note_id" TEXT NOT NULL,
  "user_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "subject" TEXT NOT NULL,
  "predicate" TEXT NOT NULL,
  "object" TEXT NOT NULL,
  "created_at" BIGINT NOT NULL,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("note_id") REFERENCES "notes" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "note_share_tokens" (
  "id" TEXT,
  "note_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "owner_id" TEXT NOT NULL,
  "role" TEXT NOT NULL DEFAULT 'viewer',
  "token_hash" TEXT NOT NULL,
  "token_prefix" TEXT NOT NULL,
  "max_uses" BIGINT,
  "uses" BIGINT NOT NULL DEFAULT 0,
  "expires_at" BIGINT,
  "revoked_at" BIGINT,
  "created_by" TEXT NOT NULL,
  "created_at" BIGINT NOT NULL,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("note_id") REFERENCES "notes" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "note_shares" (
  "id" TEXT,
  "note_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "owner_id" TEXT NOT NULL,
  "user_id" TEXT NOT NULL,
  "role" TEXT NOT NULL DEFAULT 'viewer',
  "joined_at" BIGINT NOT NULL,
  "invited_via_token_id" TEXT,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("note_id") REFERENCES "notes" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "note_suggestions" (
  "id" TEXT,
  "note_id" TEXT NOT NULL,
  "doc_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "author_kind" TEXT NOT NULL DEFAULT 'agent',
  "author_id" TEXT NOT NULL,
  "author_site" TEXT NOT NULL,
  "action" TEXT NOT NULL,
  "status" TEXT NOT NULL DEFAULT 'pending',
  "ops_json" TEXT NOT NULL,
  "preview_text" TEXT NOT NULL DEFAULT '',
  "anchor_json" TEXT,
  "created_at" BIGINT NOT NULL,
  "resolved_at" BIGINT,
  "resolved_by" TEXT,
  "before_text" TEXT NOT NULL DEFAULT '',
  PRIMARY KEY ("id"),
  FOREIGN KEY ("note_id") REFERENCES "notes" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "note_synced_blocks" (
  "id" TEXT,
  "note_id" TEXT NOT NULL,
  "user_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "source_note_id" TEXT NOT NULL,
  "source_block_id" TEXT NOT NULL DEFAULT '',
  "created_at" BIGINT NOT NULL,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("note_id") REFERENCES "notes" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "note_versions" (
  "id" TEXT,
  "note_id" TEXT NOT NULL,
  "user_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "title" TEXT NOT NULL,
  "doc_json" TEXT NOT NULL,
  "label" TEXT,
  "reason" TEXT NOT NULL DEFAULT 'manual',
  "word_count" BIGINT NOT NULL DEFAULT 0,
  "created_by" TEXT NOT NULL,
  "created_at" BIGINT NOT NULL,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("note_id") REFERENCES "notes" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "notification_feed" (
  "id" TEXT,
  "tenant_id" TEXT,
  "principal_id" TEXT NOT NULL,
  "category" TEXT NOT NULL,
  "title" TEXT NOT NULL,
  "body" TEXT,
  "deep_link" TEXT,
  "priority" TEXT NOT NULL DEFAULT 'normal',
  "dedupe_key" TEXT,
  "created_at" BIGINT NOT NULL,
  "read_at" BIGINT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "notification_outbox" (
  "id" TEXT,
  "run_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "user_id" TEXT NOT NULL,
  "channels" TEXT NOT NULL,
  "payload" TEXT NOT NULL,
  "idempotency_key" TEXT NOT NULL,
  "status" TEXT NOT NULL DEFAULT 'pending',
  "attempts" BIGINT NOT NULL DEFAULT 0,
  "lease_until" BIGINT,
  "next_attempt_at" BIGINT NOT NULL,
  "last_error" TEXT,
  "created_at" BIGINT NOT NULL,
  "sent_at" BIGINT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "notification_preferences" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "categories" TEXT NOT NULL DEFAULT '[]',
  "quiet_hours" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "timezone" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "oauth_flow_states" (
  "id" TEXT,
  "state_key" TEXT NOT NULL,
  "user_id" TEXT,
  "provider" TEXT NOT NULL,
  "expires_at" TEXT NOT NULL,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "oauth_linked_accounts" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "provider" TEXT NOT NULL,
  "provider_user_id" TEXT NOT NULL,
  "email" TEXT NOT NULL,
  "name" TEXT NOT NULL,
  "picture_url" TEXT,
  "linked_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "last_used_at" TEXT,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "passkey_credentials" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "credential_id" TEXT NOT NULL,
  "public_key_cose" TEXT NOT NULL,
  "aaguid" TEXT NOT NULL DEFAULT '',
  "counter" BIGINT NOT NULL DEFAULT 0,
  "transports" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "last_used_at" TEXT,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "plugin_configs" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "plugin_type" TEXT NOT NULL DEFAULT 'community',
  "package_name" TEXT NOT NULL,
  "version" TEXT NOT NULL DEFAULT '1.0.0',
  "capabilities" TEXT,
  "trust_level" TEXT NOT NULL DEFAULT 'community',
  "auto_update" BIGINT NOT NULL DEFAULT 0,
  "config" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "procedural_memory" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "agent_id" TEXT NOT NULL DEFAULT 'default',
  "instruction_delta" TEXT NOT NULL,
  "proposed_by" TEXT NOT NULL DEFAULT 'consolidation-curator',
  "status" TEXT NOT NULL DEFAULT 'proposed',
  "confidence" DOUBLE PRECISION NOT NULL DEFAULT 0.7,
  "human_task_id" TEXT,
  "applied_at" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "prompt_contracts" (
  "id" TEXT,
  "key" TEXT NOT NULL,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "contract_type" TEXT NOT NULL,
  "schema" TEXT,
  "config" TEXT NOT NULL DEFAULT '{}',
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "realm" TEXT NOT NULL DEFAULT 'global',
  "owner_tenant_id" TEXT,
  "logical_key" TEXT,
  "origin_id" TEXT,
  "origin_hash" TEXT,
  "content_hash" TEXT NOT NULL DEFAULT '',
  "track_mode" TEXT NOT NULL DEFAULT 'pin',
  "share_mode" TEXT NOT NULL DEFAULT 'private',
  "deprecated_at" TEXT,
  "deprecation_note" TEXT,
  "superseded_by_id" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "prompts" (
  "id" TEXT,
  "key" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "category" TEXT,
  "prompt_type" TEXT NOT NULL DEFAULT 'template',
  "owner" TEXT,
  "status" TEXT NOT NULL DEFAULT 'published',
  "tags" TEXT,
  "template" TEXT NOT NULL,
  "variables" TEXT,
  "version" TEXT NOT NULL DEFAULT '1.0',
  "model_compatibility" TEXT,
  "execution_defaults" TEXT,
  "framework" TEXT,
  "metadata" TEXT,
  "is_default" BIGINT NOT NULL DEFAULT 0,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "realm" TEXT NOT NULL DEFAULT 'global',
  "owner_tenant_id" TEXT,
  "logical_key" TEXT,
  "origin_id" TEXT,
  "origin_hash" TEXT,
  "content_hash" TEXT NOT NULL DEFAULT '',
  "track_mode" TEXT NOT NULL DEFAULT 'pin',
  "share_mode" TEXT NOT NULL DEFAULT 'private',
  "deprecated_at" TEXT,
  "deprecation_note" TEXT,
  "superseded_by_id" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "prompt_eval_datasets" (
  "id" TEXT,
  "prompt_id" TEXT NOT NULL,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "prompt_version" TEXT,
  "status" TEXT NOT NULL DEFAULT 'draft',
  "pass_threshold" DOUBLE PRECISION NOT NULL DEFAULT 0.75,
  "cases_json" TEXT NOT NULL DEFAULT '[]',
  "rubric_json" TEXT,
  "metadata" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("prompt_id") REFERENCES "prompts" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "prompt_eval_runs" (
  "id" TEXT,
  "dataset_id" TEXT NOT NULL,
  "prompt_id" TEXT NOT NULL,
  "prompt_version" TEXT NOT NULL,
  "status" TEXT NOT NULL DEFAULT 'pending',
  "avg_score" DOUBLE PRECISION NOT NULL DEFAULT 0,
  "passed_cases" BIGINT NOT NULL DEFAULT 0,
  "failed_cases" BIGINT NOT NULL DEFAULT 0,
  "total_cases" BIGINT NOT NULL DEFAULT 0,
  "results_json" TEXT NOT NULL DEFAULT '[]',
  "summary_json" TEXT,
  "metadata" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "completed_at" TEXT,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("prompt_id") REFERENCES "prompts" ("id") ON DELETE CASCADE,
  FOREIGN KEY ("dataset_id") REFERENCES "prompt_eval_datasets" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "prompt_experiments" (
  "id" TEXT,
  "prompt_id" TEXT NOT NULL,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "status" TEXT NOT NULL DEFAULT 'draft',
  "variants_json" TEXT NOT NULL DEFAULT '[]',
  "assignment_key_template" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("prompt_id") REFERENCES "prompts" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "prompt_fragments" (
  "id" TEXT,
  "key" TEXT NOT NULL,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "category" TEXT,
  "content" TEXT NOT NULL,
  "variables" TEXT,
  "tags" TEXT,
  "version" TEXT NOT NULL DEFAULT '1.0',
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "realm" TEXT NOT NULL DEFAULT 'global',
  "owner_tenant_id" TEXT,
  "logical_key" TEXT,
  "origin_id" TEXT,
  "origin_hash" TEXT,
  "content_hash" TEXT NOT NULL DEFAULT '',
  "track_mode" TEXT NOT NULL DEFAULT 'pin',
  "share_mode" TEXT NOT NULL DEFAULT 'private',
  "deprecated_at" TEXT,
  "deprecation_note" TEXT,
  "superseded_by_id" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "prompt_frameworks" (
  "id" TEXT,
  "key" TEXT NOT NULL,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "sections" TEXT NOT NULL DEFAULT '[]',
  "section_separator" TEXT NOT NULL DEFAULT '

',
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "realm" TEXT NOT NULL DEFAULT 'global',
  "owner_tenant_id" TEXT,
  "logical_key" TEXT,
  "origin_id" TEXT,
  "origin_hash" TEXT,
  "content_hash" TEXT NOT NULL DEFAULT '',
  "track_mode" TEXT NOT NULL DEFAULT 'pin',
  "share_mode" TEXT NOT NULL DEFAULT 'private',
  "deprecated_at" TEXT,
  "deprecation_note" TEXT,
  "superseded_by_id" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "prompt_optimizers" (
  "id" TEXT,
  "key" TEXT NOT NULL,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "implementation_kind" TEXT NOT NULL DEFAULT 'rule',
  "config" TEXT NOT NULL DEFAULT '{}',
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "prompt_optimization_runs" (
  "id" TEXT,
  "prompt_id" TEXT NOT NULL,
  "source_version" TEXT NOT NULL,
  "candidate_version" TEXT NOT NULL,
  "optimizer_id" TEXT,
  "objective" TEXT NOT NULL,
  "source_template" TEXT NOT NULL,
  "candidate_template" TEXT NOT NULL,
  "diff_json" TEXT NOT NULL,
  "eval_baseline_json" TEXT,
  "eval_candidate_json" TEXT,
  "status" TEXT NOT NULL DEFAULT 'completed',
  "metadata" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("optimizer_id") REFERENCES "prompt_optimizers" ("id") ON DELETE SET NULL,
  FOREIGN KEY ("prompt_id") REFERENCES "prompts" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "prompt_strategies" (
  "id" TEXT,
  "key" TEXT NOT NULL,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "instruction_prefix" TEXT,
  "instruction_suffix" TEXT,
  "config" TEXT NOT NULL DEFAULT '{}',
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "realm" TEXT NOT NULL DEFAULT 'global',
  "owner_tenant_id" TEXT,
  "logical_key" TEXT,
  "origin_id" TEXT,
  "origin_hash" TEXT,
  "content_hash" TEXT NOT NULL DEFAULT '',
  "track_mode" TEXT NOT NULL DEFAULT 'pin',
  "share_mode" TEXT NOT NULL DEFAULT 'private',
  "deprecated_at" TEXT,
  "deprecation_note" TEXT,
  "superseded_by_id" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "prompt_suggestion_events" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "prompt_id" TEXT NOT NULL,
  "title" TEXT,
  "source" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "prompt_versions" (
  "id" TEXT,
  "prompt_id" TEXT NOT NULL,
  "version" TEXT NOT NULL,
  "status" TEXT NOT NULL DEFAULT 'draft',
  "template" TEXT NOT NULL,
  "variables" TEXT,
  "model_compatibility" TEXT,
  "execution_defaults" TEXT,
  "framework" TEXT,
  "metadata" TEXT,
  "is_active" BIGINT NOT NULL DEFAULT 0,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("prompt_id") REFERENCES "prompts" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "provider_tool_adapters" (
  "id" TEXT,
  "provider" TEXT NOT NULL,
  "display_name" TEXT NOT NULL,
  "adapter_module" TEXT NOT NULL,
  "tool_format" TEXT NOT NULL,
  "tool_call_response_format" TEXT NOT NULL,
  "tool_result_format" TEXT NOT NULL,
  "system_prompt_location" TEXT NOT NULL DEFAULT 'system_message',
  "name_validation_regex" TEXT NOT NULL DEFAULT '^[a-zA-Z0-9_-]{1,64}$',
  "max_tool_count" BIGINT NOT NULL DEFAULT 128,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "realm" TEXT NOT NULL DEFAULT 'global',
  "owner_tenant_id" TEXT,
  "logical_key" TEXT,
  "origin_id" TEXT,
  "origin_hash" TEXT,
  "content_hash" TEXT NOT NULL DEFAULT '',
  "track_mode" TEXT NOT NULL DEFAULT 'pin',
  "share_mode" TEXT NOT NULL DEFAULT 'private',
  "deprecated_at" TEXT,
  "deprecation_note" TEXT,
  "superseded_by_id" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "realm_proposals" (
  "id" TEXT,
  "family" TEXT NOT NULL,
  "logical_key" TEXT NOT NULL,
  "fork_id" TEXT NOT NULL,
  "tenant_id" TEXT NOT NULL,
  "note" TEXT,
  "status" TEXT NOT NULL DEFAULT 'pending',
  "proposed_by" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "reviewed_at" TEXT,
  "reviewed_by" TEXT,
  "review_note" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "realm_tenant_state" (
  "id" TEXT,
  "tenant_id" TEXT NOT NULL,
  "family" TEXT NOT NULL,
  "logical_key" TEXT NOT NULL,
  "enabled" BIGINT,
  "priority" BIGINT,
  "pinned_version" BIGINT,
  "updated_at" TEXT NOT NULL,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "realm_versions" (
  "id" TEXT,
  "family" TEXT NOT NULL,
  "logical_key" TEXT NOT NULL,
  "version" BIGINT NOT NULL,
  "content_hash" TEXT NOT NULL,
  "payload" TEXT NOT NULL,
  "published_by" TEXT,
  "note" TEXT,
  "published_at" TEXT NOT NULL,
  PRIMARY KEY ("id")
);

-- Upgrade Engine — migration ledger + run persistence (mirrors SQLite: runner-managed schema_migrations,
-- m163 upgrade_runs/upgrade_details). Postgres applies this schema declaratively (idempotent), so its
-- "ledger" of applied DDL is the schema itself; the shared run/detail persistence is dialect-neutral.
CREATE TABLE IF NOT EXISTS "schema_migrations" (
  "id" TEXT PRIMARY KEY,
  "hash" TEXT NOT NULL,
  "applied_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS')
);

CREATE TABLE IF NOT EXISTS "upgrade_runs" (
  "id" TEXT,
  "mode" TEXT NOT NULL,
  "status" TEXT NOT NULL,
  "from_version" TEXT,
  "to_version" TEXT,
  "dialect" TEXT,
  "summary_json" TEXT NOT NULL DEFAULT '{}',
  "started_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "finished_at" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "upgrade_details" (
  "id" TEXT,
  "run_id" TEXT NOT NULL,
  "family" TEXT NOT NULL,
  "logical_key" TEXT NOT NULL,
  "layer" TEXT NOT NULL DEFAULT 'L4',
  "disposition" TEXT NOT NULL,
  "priority" TEXT NOT NULL DEFAULT 'P3',
  "base_hash" TEXT,
  "local_hash" TEXT,
  "remote_hash" TEXT,
  "note" TEXT,
  "resolution" TEXT,
  "resolved_at" TEXT,
  "resolved_by" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);
CREATE INDEX IF NOT EXISTS ix_upgrade_details_run ON upgrade_details(run_id);
CREATE INDEX IF NOT EXISTS ix_upgrade_details_family_key ON upgrade_details(family, logical_key);
CREATE INDEX IF NOT EXISTS ix_upgrade_details_priority ON upgrade_details(priority);
CREATE INDEX IF NOT EXISTS ix_upgrade_details_disposition ON upgrade_details(disposition);
CREATE INDEX IF NOT EXISTS ix_upgrade_details_propagation ON upgrade_details(family, logical_key, remote_hash);

CREATE TABLE IF NOT EXISTS "upgrade_releases" (
  "id" TEXT,
  "name" TEXT,
  "version" TEXT NOT NULL,
  "edition" TEXT,
  "channel" TEXT,
  "published_at" TEXT,
  "expires_at" TEXT,
  "key_fingerprint" TEXT,
  "outcome" TEXT NOT NULL,
  "reject_reason" TEXT,
  "accepted" BIGINT NOT NULL DEFAULT 0,
  "manifest_json" TEXT,
  "checked_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);
CREATE INDEX IF NOT EXISTS idx_upgrade_releases_accepted ON upgrade_releases(accepted, version);
CREATE INDEX IF NOT EXISTS idx_upgrade_releases_checked ON upgrade_releases(checked_at);

CREATE TABLE IF NOT EXISTS "recipe_configs" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "recipe_type" TEXT NOT NULL DEFAULT 'workflow',
  "model" TEXT,
  "provider" TEXT,
  "system_prompt" TEXT,
  "tools" TEXT,
  "guardrails" TEXT,
  "max_steps" BIGINT DEFAULT 10,
  "options" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "reliability_policies" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "policy_type" TEXT NOT NULL DEFAULT 'retry',
  "max_retries" BIGINT,
  "initial_delay_ms" BIGINT,
  "max_delay_ms" BIGINT,
  "backoff_multiplier" DOUBLE PRECISION,
  "max_concurrent" BIGINT,
  "queue_size" BIGINT,
  "strategy" TEXT,
  "ttl_ms" BIGINT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "replay_scenarios" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "golden_prompt" TEXT NOT NULL,
  "golden_response" TEXT NOT NULL,
  "model" TEXT,
  "provider" TEXT,
  "tags" TEXT,
  "acceptance_criteria" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "routing_capability_signals" (
  "id" TEXT,
  "tenant_id" TEXT,
  "model_id" TEXT NOT NULL,
  "provider" TEXT NOT NULL,
  "task_key" TEXT NOT NULL,
  "source" TEXT NOT NULL,
  "signal_type" TEXT NOT NULL,
  "value" DOUBLE PRECISION NOT NULL,
  "weight" DOUBLE PRECISION NOT NULL DEFAULT 1.0,
  "evidence_id" TEXT,
  "message_id" TEXT,
  "trace_id" TEXT,
  "metadata" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "routing_decision_traces" (
  "id" TEXT,
  "tenant_id" TEXT,
  "agent_id" TEXT,
  "workflow_step_id" TEXT,
  "task_key" TEXT,
  "inference_source" TEXT,
  "selected_model_id" TEXT NOT NULL,
  "selected_provider" TEXT NOT NULL,
  "selected_capability_score" DOUBLE PRECISION,
  "weights_used" TEXT NOT NULL,
  "candidate_breakdown" TEXT NOT NULL,
  "tool_translation_applied" BIGINT NOT NULL DEFAULT 0,
  "source_provider" TEXT,
  "estimated_cost_usd" DOUBLE PRECISION,
  "decided_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "routing_experiments" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "tenant_id" TEXT,
  "task_key" TEXT,
  "baseline_provider" TEXT NOT NULL,
  "baseline_model_id" TEXT NOT NULL,
  "candidate_provider" TEXT NOT NULL,
  "candidate_model_id" TEXT NOT NULL,
  "traffic_pct" DOUBLE PRECISION NOT NULL DEFAULT 10,
  "status" TEXT NOT NULL DEFAULT 'active',
  "metadata" TEXT,
  "started_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "ended_at" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "routing_policies" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "strategy" TEXT NOT NULL DEFAULT 'priority',
  "constraints" TEXT,
  "weights" TEXT,
  "fallback_model" TEXT,
  "fallback_provider" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "fallback_chain" TEXT,
  "realm" TEXT NOT NULL DEFAULT 'global',
  "owner_tenant_id" TEXT,
  "logical_key" TEXT,
  "origin_id" TEXT,
  "origin_hash" TEXT,
  "content_hash" TEXT NOT NULL DEFAULT '',
  "track_mode" TEXT NOT NULL DEFAULT 'pin',
  "share_mode" TEXT NOT NULL DEFAULT 'private',
  "deprecated_at" TEXT,
  "deprecation_note" TEXT,
  "superseded_by_id" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "routing_surface_items" (
  "id" TEXT,
  "kind" TEXT NOT NULL,
  "severity" TEXT NOT NULL,
  "model_id" TEXT NOT NULL,
  "provider" TEXT NOT NULL,
  "task_key" TEXT NOT NULL,
  "tenant_id" TEXT,
  "message" TEXT NOT NULL,
  "metric_7d" DOUBLE PRECISION,
  "metric_30d" DOUBLE PRECISION,
  "drop_pct" DOUBLE PRECISION,
  "sample_count_7d" BIGINT,
  "sample_count_30d" BIGINT,
  "auto_disabled" BIGINT NOT NULL DEFAULT 0,
  "status" TEXT NOT NULL DEFAULT 'open',
  "resolution_note" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "resolved_at" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "run_annotations" (
  "id" TEXT,
  "run_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "part_id" TEXT NOT NULL DEFAULT '',
  "author_id" TEXT NOT NULL,
  "name" TEXT NOT NULL,
  "data_type" TEXT NOT NULL DEFAULT 'numeric',
  "value" DOUBLE PRECISION,
  "string_value" TEXT,
  "comment" TEXT,
  "source" TEXT NOT NULL DEFAULT 'human',
  "created_at" BIGINT NOT NULL,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("run_id") REFERENCES "user_runs" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "run_comments" (
  "id" TEXT,
  "run_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "thread_id" TEXT NOT NULL,
  "parent_id" TEXT,
  "author_id" TEXT NOT NULL,
  "body" TEXT NOT NULL,
  "body_html" TEXT NOT NULL,
  "mentions_json" TEXT NOT NULL DEFAULT '[]',
  "anchor_part_id" TEXT NOT NULL DEFAULT '',
  "anchor_seq" BIGINT NOT NULL DEFAULT 0,
  "anchor_range_json" TEXT,
  "created_at" BIGINT NOT NULL,
  "updated_at" BIGINT NOT NULL,
  "edited_at" BIGINT,
  "deleted_at" BIGINT,
  "deleted_by" TEXT,
  "resolved_at" BIGINT,
  "resolved_by" TEXT,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("run_id") REFERENCES "user_runs" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "run_embeddings" (
  "run_id" TEXT,
  "user_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "dim" BIGINT NOT NULL,
  "embedding_json" TEXT NOT NULL,
  "content_hash" TEXT NOT NULL,
  "title" TEXT,
  "content" TEXT,
  "updated_at" BIGINT NOT NULL,
  PRIMARY KEY ("run_id")
);

CREATE TABLE IF NOT EXISTS "run_presence" (
  "id" TEXT,
  "run_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "user_id" TEXT NOT NULL,
  "display_name" TEXT NOT NULL,
  "presence" TEXT NOT NULL DEFAULT 'online',
  "peer_type" TEXT NOT NULL DEFAULT 'human',
  "color" TEXT,
  "cursor_json" TEXT,
  "last_heartbeat_at" BIGINT NOT NULL,
  "expires_at" BIGINT NOT NULL,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("run_id") REFERENCES "user_runs" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "run_public_shares" (
  "id" TEXT,
  "run_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "token_hash" TEXT NOT NULL,
  "token_prefix" TEXT NOT NULL,
  "created_by" TEXT NOT NULL,
  "created_at" BIGINT NOT NULL,
  "expires_at" BIGINT,
  "revoked_at" BIGINT,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("run_id") REFERENCES "user_runs" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "run_stream_config" (
  "id" TEXT DEFAULT 'global',
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "heartbeat_ms" BIGINT NOT NULL DEFAULT 15000,
  "max_reconnects" BIGINT NOT NULL DEFAULT 8,
  "backoff_ms" TEXT NOT NULL DEFAULT '[250,500,1000,2000,4000,8000,16000,30000]',
  "stall_timeout_ms" BIGINT NOT NULL DEFAULT 60000,
  "throttle_ms" BIGINT NOT NULL DEFAULT 50,
  "journal_retention_hours" BIGINT NOT NULL DEFAULT 24,
  "journal_max_events" BIGINT NOT NULL DEFAULT 2000,
  "resume_window_seconds" BIGINT NOT NULL DEFAULT 900,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "run_subscriptions" (
  "id" TEXT,
  "run_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "user_id" TEXT NOT NULL,
  "channels" TEXT NOT NULL DEFAULT '["inapp"]',
  "created_at" BIGINT NOT NULL,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("run_id") REFERENCES "user_runs" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "sandbox_policies" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "max_cpu_ms" BIGINT,
  "max_memory_mb" BIGINT,
  "max_duration_ms" BIGINT NOT NULL DEFAULT 30000,
  "max_output_bytes" BIGINT,
  "allowed_modules" TEXT,
  "denied_modules" TEXT,
  "network_access" BIGINT NOT NULL DEFAULT 0,
  "filesystem_access" TEXT NOT NULL DEFAULT 'none',
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "scaffold_templates" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "template_type" TEXT NOT NULL DEFAULT 'basic-agent',
  "files" TEXT,
  "dependencies" TEXT,
  "dev_dependencies" TEXT,
  "variables" TEXT,
  "post_install" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "realm" TEXT NOT NULL DEFAULT 'global',
  "owner_tenant_id" TEXT,
  "logical_key" TEXT,
  "origin_id" TEXT,
  "origin_hash" TEXT,
  "content_hash" TEXT NOT NULL DEFAULT '',
  "track_mode" TEXT NOT NULL DEFAULT 'pin',
  "share_mode" TEXT NOT NULL DEFAULT 'private',
  "deprecated_at" TEXT,
  "deprecation_note" TEXT,
  "superseded_by_id" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "scheduled_note_agent_runs" (
  "id" TEXT,
  "agent_id" TEXT NOT NULL,
  "user_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "trigger" TEXT NOT NULL DEFAULT 'manual',
  "status" TEXT NOT NULL DEFAULT 'running',
  "started_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "finished_at" TEXT,
  "steps" BIGINT NOT NULL DEFAULT 0,
  "tokens_used" BIGINT NOT NULL DEFAULT 0,
  "notes_scanned" BIGINT NOT NULL DEFAULT 0,
  "suggestions_created" BIGINT NOT NULL DEFAULT 0,
  "output_note_id" TEXT,
  "summary" TEXT,
  "error" TEXT,
  "detail_json" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "scheduled_note_agents" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "name" TEXT NOT NULL,
  "recipe" TEXT NOT NULL DEFAULT 'daily_digest',
  "task_prompt" TEXT NOT NULL DEFAULT '',
  "trigger_type" TEXT NOT NULL DEFAULT 'schedule',
  "cron" TEXT NOT NULL DEFAULT '0 8 * * *',
  "timezone" TEXT NOT NULL DEFAULT 'UTC',
  "scope" TEXT NOT NULL DEFAULT 'recent',
  "scope_tag" TEXT NOT NULL DEFAULT '',
  "lookback_days" BIGINT NOT NULL DEFAULT 1,
  "max_notes" BIGINT NOT NULL DEFAULT 25,
  "token_budget" BIGINT NOT NULL DEFAULT 8000,
  "max_steps" BIGINT NOT NULL DEFAULT 8,
  "require_approval" BIGINT NOT NULL DEFAULT 1,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "last_run_id" TEXT,
  "last_run_at" TEXT,
  "next_run_at" BIGINT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "scope_access_log" (
  "id" TEXT,
  "event_type" TEXT NOT NULL,
  "from_scope" TEXT,
  "to_scope" TEXT,
  "skill_id" TEXT,
  "tool_name" TEXT,
  "session_id" TEXT,
  "task_id" TEXT,
  "user_id" TEXT,
  "allowed" BIGINT NOT NULL,
  "reason" TEXT,
  "delegation_chain_json" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "scope_cross_policies" (
  "id" TEXT,
  "from_scope" TEXT NOT NULL,
  "to_scope" TEXT NOT NULL,
  "allowed" BIGINT NOT NULL DEFAULT 0,
  "requires_a2a" BIGINT NOT NULL DEFAULT 1,
  "max_delegation_depth" BIGINT NOT NULL DEFAULT 1,
  "conditions_json" TEXT,
  "audit_level" TEXT NOT NULL DEFAULT 'log',
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "scope_live_agent_assignments" (
  "scope_id" TEXT NOT NULL,
  "mesh_key" TEXT NOT NULL,
  "role_key" TEXT NOT NULL DEFAULT '',
  PRIMARY KEY ("scope_id", "mesh_key", "role_key")
);

CREATE TABLE IF NOT EXISTS "scope_skill_assignments" (
  "scope_id" TEXT NOT NULL,
  "skill_id" TEXT NOT NULL,
  PRIMARY KEY ("scope_id", "skill_id")
);

CREATE TABLE IF NOT EXISTS "search_providers" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "provider_type" TEXT NOT NULL,
  "api_key" TEXT,
  "base_url" TEXT,
  "priority" BIGINT NOT NULL DEFAULT 0,
  "options" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "credentials_encrypted" BIGINT NOT NULL DEFAULT 0,
  "api_key_enc" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "semantic_cache_config" (
  "id" TEXT DEFAULT 'global',
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "embedding_model" TEXT NOT NULL DEFAULT 'text-embedding-3-small',
  "embedding_version" TEXT NOT NULL DEFAULT 'v1',
  "similarity_threshold" DOUBLE PRECISION NOT NULL DEFAULT 0.92,
  "invalidation_radius" DOUBLE PRECISION NOT NULL DEFAULT 0.95,
  "max_entries" BIGINT NOT NULL DEFAULT 1000,
  "ttl_ms" BIGINT NOT NULL DEFAULT 600000,
  "scope" TEXT NOT NULL DEFAULT 'user',
  "bypass_patterns" TEXT,
  "verified_bounds" BIGINT NOT NULL DEFAULT 0,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "semantic_memory" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "chat_id" TEXT,
  "tenant_id" TEXT,
  "content" TEXT NOT NULL,
  "memory_type" TEXT NOT NULL DEFAULT 'semantic',
  "source" TEXT NOT NULL DEFAULT 'assistant',
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "embedding" TEXT,
  "metadata" TEXT,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("chat_id") REFERENCES "chats" ("id") ON DELETE SET NULL,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "shared_sessions" (
  "id" TEXT,
  "run_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "owner_id" TEXT NOT NULL,
  "status" TEXT NOT NULL DEFAULT 'live',
  "max_participants" BIGINT NOT NULL DEFAULT 50,
  "created_at" BIGINT NOT NULL,
  "ended_at" BIGINT,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("run_id") REFERENCES "user_runs" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "session_participants" (
  "id" TEXT,
  "session_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "user_id" TEXT NOT NULL,
  "role" TEXT NOT NULL DEFAULT 'viewer',
  "joined_at" BIGINT NOT NULL,
  "invited_via_token_id" TEXT,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("session_id") REFERENCES "shared_sessions" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "session_share_tokens" (
  "id" TEXT,
  "session_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "role" TEXT NOT NULL DEFAULT 'viewer',
  "token_hash" TEXT NOT NULL,
  "token_prefix" TEXT NOT NULL,
  "max_uses" BIGINT,
  "uses" BIGINT NOT NULL DEFAULT 0,
  "expires_at" BIGINT,
  "revoked_at" BIGINT,
  "created_by" TEXT NOT NULL,
  "created_at" BIGINT NOT NULL,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("session_id") REFERENCES "shared_sessions" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "sessions" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "csrf_token" TEXT NOT NULL,
  "expires_at" TEXT NOT NULL,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "mfa_verified_at" TEXT,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("user_id") REFERENCES "users" ("id")
);

CREATE TABLE IF NOT EXISTS "skills" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT NOT NULL DEFAULT '',
  "category" TEXT NOT NULL DEFAULT 'general',
  "trigger_patterns" TEXT NOT NULL DEFAULT '[]',
  "instructions" TEXT NOT NULL DEFAULT '',
  "tool_names" TEXT,
  "examples" TEXT,
  "tags" TEXT,
  "priority" BIGINT NOT NULL DEFAULT 0,
  "version" TEXT NOT NULL DEFAULT '1.0',
  "tool_policy_key" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "supervisor_agent_id" TEXT,
  "domain_sections" TEXT,
  "execution_contract" TEXT,
  "provides" TEXT NOT NULL DEFAULT '[]',
  "requires" TEXT NOT NULL DEFAULT '[]',
  "precondition" TEXT NOT NULL DEFAULT '[]',
  "composes_with" TEXT NOT NULL DEFAULT '[]',
  "conflicts_with" TEXT NOT NULL DEFAULT '[]',
  "trust" BIGINT NOT NULL DEFAULT 0,
  "input_modalities" TEXT NOT NULL DEFAULT '["text"]',
  "trust_tier" BIGINT NOT NULL DEFAULT 1,
  "realm" TEXT NOT NULL DEFAULT 'global',
  "owner_tenant_id" TEXT,
  "logical_key" TEXT,
  "origin_id" TEXT,
  "origin_hash" TEXT,
  "content_hash" TEXT NOT NULL DEFAULT '',
  "track_mode" TEXT NOT NULL DEFAULT 'pin',
  "share_mode" TEXT NOT NULL DEFAULT 'private',
  "deprecated_at" TEXT,
  "deprecation_note" TEXT,
  "superseded_by_id" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "social_accounts" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "platform" TEXT NOT NULL,
  "api_key" TEXT,
  "api_secret" TEXT,
  "access_token" TEXT,
  "refresh_token" TEXT,
  "token_expires_at" TEXT,
  "oauth_state" TEXT,
  "status" TEXT NOT NULL DEFAULT 'disconnected',
  "base_url" TEXT,
  "options" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "credentials_encrypted" BIGINT NOT NULL DEFAULT 0,
  "api_key_enc" TEXT,
  "api_secret_enc" TEXT,
  "access_token_enc" TEXT,
  "refresh_token_enc" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "sso_linked_accounts" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "identity_provider" TEXT NOT NULL,
  "email" TEXT,
  "session_encrypted" TEXT NOT NULL,
  "encryption_iv" TEXT NOT NULL,
  "status" TEXT NOT NULL DEFAULT 'active',
  "linked_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "starter_prompts" (
  "id" TEXT,
  "surface_id" TEXT NOT NULL,
  "label" TEXT NOT NULL,
  "prompt_text" TEXT NOT NULL,
  "sort_order" BIGINT NOT NULL DEFAULT 0,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "metadata" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "system_attestation_signing_key" (
  "key" TEXT,
  "private_key_pem" TEXT NOT NULL,
  "public_key_pem" TEXT NOT NULL,
  "fingerprint" TEXT NOT NULL,
  "created_at" BIGINT NOT NULL,
  PRIMARY KEY ("key")
);

CREATE TABLE IF NOT EXISTS "task_contracts" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "input_schema" TEXT,
  "output_schema" TEXT,
  "acceptance_criteria" TEXT NOT NULL DEFAULT '[]',
  "max_attempts" BIGINT,
  "timeout_ms" BIGINT,
  "evidence_required" TEXT,
  "min_confidence" DOUBLE PRECISION,
  "require_human_review" BIGINT NOT NULL DEFAULT 0,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "task_type_definitions" (
  "id" TEXT,
  "task_key" TEXT NOT NULL,
  "display_name" TEXT NOT NULL,
  "category" TEXT NOT NULL,
  "description" TEXT NOT NULL DEFAULT '',
  "output_modality" TEXT NOT NULL,
  "default_strategy" TEXT NOT NULL,
  "default_weights" TEXT NOT NULL DEFAULT '{"cost":0.25,"speed":0.25,"quality":0.25,"capability":0.25}',
  "inference_hints" TEXT NOT NULL DEFAULT '{}',
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "realm" TEXT NOT NULL DEFAULT 'global',
  "owner_tenant_id" TEXT,
  "logical_key" TEXT,
  "origin_id" TEXT,
  "origin_hash" TEXT,
  "content_hash" TEXT NOT NULL DEFAULT '',
  "track_mode" TEXT NOT NULL DEFAULT 'pin',
  "share_mode" TEXT NOT NULL DEFAULT 'private',
  "deprecated_at" TEXT,
  "deprecation_note" TEXT,
  "superseded_by_id" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "task_type_tenant_overrides" (
  "id" TEXT,
  "tenant_id" TEXT NOT NULL,
  "task_key" TEXT NOT NULL,
  "weights" TEXT,
  "preferred_model_id" TEXT,
  "preferred_provider" TEXT,
  "preferred_boost_pct" DOUBLE PRECISION NOT NULL DEFAULT 20,
  "cost_ceiling_per_call" DOUBLE PRECISION,
  "optimisation_strategy" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "temporal_reminders" (
  "id" TEXT NOT NULL,
  "scope_id" TEXT NOT NULL,
  "text" TEXT NOT NULL,
  "due_at" TEXT NOT NULL,
  "timezone" TEXT NOT NULL,
  "status" TEXT NOT NULL DEFAULT 'scheduled',
  "created_at" TEXT NOT NULL,
  "cancelled_at" TEXT,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("scope_id", "id")
);

CREATE TABLE IF NOT EXISTS "temporal_stopwatches" (
  "id" TEXT NOT NULL,
  "scope_id" TEXT NOT NULL,
  "label" TEXT,
  "state" TEXT NOT NULL,
  "created_at" TEXT NOT NULL,
  "started_at" TEXT,
  "paused_at" TEXT,
  "resumed_at" TEXT,
  "stopped_at" TEXT,
  "elapsed_ms" BIGINT NOT NULL DEFAULT 0,
  "laps_json" TEXT NOT NULL DEFAULT '[]',
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("scope_id", "id")
);

CREATE TABLE IF NOT EXISTS "temporal_timers" (
  "id" TEXT NOT NULL,
  "scope_id" TEXT NOT NULL,
  "label" TEXT,
  "duration_ms" BIGINT,
  "state" TEXT NOT NULL,
  "created_at" TEXT NOT NULL,
  "started_at" TEXT,
  "paused_at" TEXT,
  "resumed_at" TEXT,
  "stopped_at" TEXT,
  "elapsed_ms" BIGINT NOT NULL DEFAULT 0,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("scope_id", "id")
);

CREATE TABLE IF NOT EXISTS "tenant_accessibility" (
  "tenant_id" TEXT,
  "announce_mode" TEXT NOT NULL DEFAULT 'summary',
  "reduced_motion" BIGINT NOT NULL DEFAULT 0,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "always_show_focus" BIGINT NOT NULL DEFAULT 0,
  "confirm_destructive" BIGINT NOT NULL DEFAULT 1,
  "show_skeletons" BIGINT NOT NULL DEFAULT 1,
  PRIMARY KEY ("tenant_id")
);

CREATE TABLE IF NOT EXISTS "tenant_ai_transparency" (
  "tenant_id" TEXT,
  "show_ai_label" BIGINT NOT NULL DEFAULT 1,
  "disclosure_text" TEXT NOT NULL DEFAULT 'AI-generated — may be inaccurate. Check anything important.',
  "content_warnings" BIGINT NOT NULL DEFAULT 1,
  "feedback_enabled" BIGINT NOT NULL DEFAULT 1,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("tenant_id")
);

CREATE TABLE IF NOT EXISTS "tenant_answer_versions" (
  "tenant_id" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "max_variants" BIGINT NOT NULL DEFAULT 5,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("tenant_id")
);

CREATE TABLE IF NOT EXISTS "tenant_appearance" (
  "tenant_id" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "brand_name" TEXT,
  "logo_svg" TEXT,
  "color_scheme" TEXT NOT NULL DEFAULT 'system',
  "variant" TEXT NOT NULL DEFAULT 'pro',
  "accent" TEXT,
  "on_accent" TEXT,
  "corner_style" TEXT NOT NULL DEFAULT 'soft',
  "font_display" TEXT,
  "font_body" TEXT,
  "density" TEXT NOT NULL DEFAULT 'comfortable',
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("tenant_id")
);

CREATE TABLE IF NOT EXISTS "tenant_artifact_settings" (
  "id" TEXT DEFAULT md5(random()::text),
  "tenant_id" TEXT NOT NULL,
  "allowed_types" TEXT,
  "max_size_bytes" BIGINT,
  "require_policy" BIGINT NOT NULL DEFAULT 0,
  "preview_enabled" BIGINT NOT NULL DEFAULT 1,
  "sandbox_html" BIGINT NOT NULL DEFAULT 1,
  "emit_enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "tenant_attestation_log" (
  "id" TEXT,
  "tenant_id" TEXT NOT NULL,
  "generated_at" BIGINT NOT NULL,
  "signature_alg" TEXT NOT NULL,
  "signature" TEXT NOT NULL,
  "signing_key_fingerprint" TEXT NOT NULL,
  "payload_hash" TEXT NOT NULL,
  "payload_json" TEXT NOT NULL,
  "requested_by" TEXT,
  "created_at" BIGINT NOT NULL,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "tenant_biks" (
  "id" TEXT,
  "tenant_id" TEXT NOT NULL,
  "epoch" BIGINT NOT NULL,
  "status" TEXT NOT NULL DEFAULT 'active',
  "wrapped" TEXT NOT NULL,
  "created_at" BIGINT NOT NULL,
  "revoked_at" BIGINT,
  "kek_id" TEXT NOT NULL DEFAULT '',
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "tenant_break_glass_request" (
  "id" TEXT,
  "tenant_id" TEXT NOT NULL,
  "requested_by" TEXT NOT NULL,
  "reason" TEXT NOT NULL,
  "status" TEXT NOT NULL DEFAULT 'pending',
  "customer_approver" TEXT,
  "approved_at" BIGINT,
  "expires_at" BIGINT NOT NULL,
  "consume_count" BIGINT NOT NULL DEFAULT 0,
  "denial_reason" TEXT,
  "created_at" BIGINT NOT NULL,
  "updated_at" BIGINT NOT NULL,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "tenant_byok_config" (
  "tenant_id" TEXT,
  "mode" TEXT NOT NULL DEFAULT 'byok',
  "public_key_pem" TEXT NOT NULL,
  "public_key_fingerprint" TEXT NOT NULL,
  "hyok_endpoint" TEXT,
  "hyok_bearer_secret_id" TEXT,
  "hyok_timeout_ms" BIGINT,
  "private_key_pem_dev" TEXT,
  "status" TEXT NOT NULL DEFAULT 'active',
  "created_by" TEXT,
  "created_at" BIGINT NOT NULL,
  "updated_at" BIGINT NOT NULL,
  "revoked_at" BIGINT,
  PRIMARY KEY ("tenant_id")
);

CREATE TABLE IF NOT EXISTS "tenant_chat_citations" (
  "tenant_id" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "min_citations" BIGINT NOT NULL DEFAULT 1,
  "scope" TEXT NOT NULL DEFAULT 'all',
  "max_sources" BIGINT NOT NULL DEFAULT 6,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("tenant_id")
);

CREATE TABLE IF NOT EXISTS "tenant_configs" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "tenant_id" TEXT NOT NULL,
  "scope" TEXT NOT NULL DEFAULT 'tenant',
  "allowed_models" TEXT,
  "denied_models" TEXT,
  "allowed_tools" TEXT,
  "max_tokens_daily" BIGINT,
  "max_cost_daily" DOUBLE PRECISION,
  "max_tokens_monthly" BIGINT,
  "max_cost_monthly" DOUBLE PRECISION,
  "features" TEXT,
  "config_overrides" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "tenant_deks" (
  "id" TEXT,
  "tenant_id" TEXT NOT NULL,
  "kek_id" TEXT NOT NULL,
  "epoch" BIGINT NOT NULL,
  "status" TEXT NOT NULL DEFAULT 'active',
  "wrapped" TEXT NOT NULL,
  "created_at" BIGINT NOT NULL,
  "rotated_at" BIGINT,
  "revoked_at" BIGINT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "tenant_deletion_requests" (
  "id" TEXT,
  "tenant_id" TEXT NOT NULL,
  "requested_at" BIGINT NOT NULL,
  "retention_until" BIGINT NOT NULL,
  "requested_by" TEXT,
  "status" TEXT NOT NULL DEFAULT 'pending',
  "purged_at" BIGINT,
  "cancelled_at" BIGINT,
  "reason" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "tenant_encryption_alert_config" (
  "id" TEXT,
  "tenant_id" TEXT,
  "kind" TEXT NOT NULL,
  "threshold" DOUBLE PRECISION NOT NULL,
  "window_ms" BIGINT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "description" TEXT,
  "created_at" BIGINT NOT NULL,
  "updated_at" BIGINT NOT NULL,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "tenant_encryption_policy" (
  "tenant_id" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 0,
  "kms_provider_id" TEXT NOT NULL DEFAULT 'local',
  "kms_config" TEXT,
  "active_kek_id" TEXT,
  "active_dek_id" TEXT,
  "active_bik_id" TEXT,
  "rotation_schedule" TEXT NOT NULL DEFAULT 'manual',
  "blind_index_enabled" BIGINT NOT NULL DEFAULT 0,
  "field_policy" TEXT NOT NULL DEFAULT '{}',
  "shred_requested_at" BIGINT,
  "shred_completed_at" BIGINT,
  "created_at" BIGINT NOT NULL DEFAULT extract(epoch from now())::bigint,
  "updated_at" BIGINT NOT NULL DEFAULT extract(epoch from now())::bigint,
  PRIMARY KEY ("tenant_id")
);

CREATE TABLE IF NOT EXISTS "tenant_governance" (
  "id" TEXT,
  "tenant_id" TEXT NOT NULL,
  "data_residency" TEXT NOT NULL DEFAULT 'unrestricted',
  "allow_model_training" BIGINT NOT NULL DEFAULT 1,
  "allow_analytics" BIGINT NOT NULL DEFAULT 1,
  "sso_required" BIGINT NOT NULL DEFAULT 0,
  "sso_protocol" TEXT NOT NULL DEFAULT 'none',
  "scim_enabled" BIGINT NOT NULL DEFAULT 0,
  "activity_retention_days" BIGINT NOT NULL DEFAULT 0,
  "audit_retention_days" BIGINT NOT NULL DEFAULT 365,
  "legal_hold" BIGINT NOT NULL DEFAULT 0,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "tenant_keks" (
  "id" TEXT,
  "tenant_id" TEXT NOT NULL,
  "version" BIGINT NOT NULL,
  "status" TEXT NOT NULL DEFAULT 'active',
  "wrapped" TEXT NOT NULL,
  "created_at" BIGINT NOT NULL,
  "rotated_at" BIGINT,
  "revoked_at" BIGINT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "tenant_locales" (
  "tenant_id" TEXT,
  "default_locale" TEXT NOT NULL DEFAULT 'en',
  "enabled_locales" TEXT NOT NULL DEFAULT '["en","es"]',
  "assistant_localized" BIGINT NOT NULL DEFAULT 0,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("tenant_id")
);

CREATE TABLE IF NOT EXISTS "tenant_role_access" (
  "tenant_id" TEXT,
  "member_dashboard" BIGINT NOT NULL DEFAULT 1,
  "member_connectors" BIGINT NOT NULL DEFAULT 0,
  "member_design" BIGINT NOT NULL DEFAULT 1,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("tenant_id")
);

CREATE TABLE IF NOT EXISTS "tenant_suggested_prompts" (
  "tenant_id" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "use_recent_notes" BIGINT NOT NULL DEFAULT 1,
  "use_recent_chats" BIGINT NOT NULL DEFAULT 1,
  "use_ai" BIGINT NOT NULL DEFAULT 1,
  "max_curated" BIGINT NOT NULL DEFAULT 4,
  "max_personalized" BIGINT NOT NULL DEFAULT 3,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("tenant_id")
);

CREATE TABLE IF NOT EXISTS "tenant_ui_translations" (
  "tenant_id" TEXT NOT NULL,
  "locale" TEXT NOT NULL,
  "messages_json" TEXT NOT NULL DEFAULT '{}',
  "source" TEXT NOT NULL DEFAULT 'ai',
  "key_count" BIGINT NOT NULL DEFAULT 0,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("tenant_id", "locale")
);

CREATE TABLE IF NOT EXISTS "tool_approval_requests" (
  "id" TEXT,
  "tool_name" TEXT NOT NULL,
  "chat_id" TEXT NOT NULL,
  "user_id" TEXT,
  "input_json" TEXT NOT NULL DEFAULT '{}',
  "policy_key" TEXT,
  "skill_key" TEXT,
  "status" TEXT NOT NULL DEFAULT 'pending',
  "requested_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "resolved_at" TEXT,
  "resolved_by" TEXT,
  "resolution_note" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "tool_audit_events" (
  "id" TEXT,
  "tool_name" TEXT NOT NULL,
  "chat_id" TEXT,
  "user_id" TEXT,
  "agent_persona" TEXT,
  "skill_key" TEXT,
  "policy_id" TEXT,
  "outcome" TEXT NOT NULL,
  "violation_reason" TEXT,
  "duration_ms" BIGINT,
  "input_preview" TEXT,
  "output_preview" TEXT,
  "error_message" TEXT,
  "metadata" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "retention_tier" TEXT NOT NULL DEFAULT 'operational',
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "tool_cache_policies" (
  "id" TEXT,
  "tool_name" TEXT NOT NULL,
  "cacheable" BIGINT NOT NULL DEFAULT 1,
  "ttl_ms" BIGINT NOT NULL DEFAULT 300000,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "tool_credentials" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "credential_type" TEXT NOT NULL DEFAULT 'api_key',
  "tool_names" TEXT,
  "env_var_name" TEXT,
  "config" TEXT,
  "rotation_due_at" TEXT,
  "validation_status" TEXT NOT NULL DEFAULT 'unknown',
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "tool_embeddings" (
  "id" TEXT,
  "tool_key" TEXT NOT NULL,
  "model_id" TEXT NOT NULL,
  "dimension" BIGINT NOT NULL,
  "embedding" TEXT NOT NULL,
  "description_hash" TEXT NOT NULL,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "tool_health_snapshots" (
  "id" TEXT,
  "tool_name" TEXT NOT NULL,
  "snapshot_at" TEXT NOT NULL,
  "invocation_count" BIGINT NOT NULL DEFAULT 0,
  "success_count" BIGINT NOT NULL DEFAULT 0,
  "error_count" BIGINT NOT NULL DEFAULT 0,
  "denied_count" BIGINT NOT NULL DEFAULT 0,
  "avg_duration_ms" DOUBLE PRECISION,
  "p95_duration_ms" DOUBLE PRECISION,
  "error_rate" DOUBLE PRECISION NOT NULL DEFAULT 0,
  "availability" DOUBLE PRECISION NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "tool_policies" (
  "id" TEXT,
  "key" TEXT NOT NULL,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "applies_to" TEXT,
  "applies_to_risk_levels" TEXT,
  "approval_required" BIGINT NOT NULL DEFAULT 0,
  "allowed_risk_levels" TEXT,
  "max_execution_ms" BIGINT,
  "rate_limit_per_minute" BIGINT,
  "max_concurrent" BIGINT,
  "require_dry_run" BIGINT NOT NULL DEFAULT 0,
  "log_input_output" BIGINT NOT NULL DEFAULT 1,
  "persona_scope" TEXT,
  "active_hours_utc" TEXT,
  "expires_at" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "realm" TEXT NOT NULL DEFAULT 'global',
  "owner_tenant_id" TEXT,
  "logical_key" TEXT,
  "origin_id" TEXT,
  "origin_hash" TEXT,
  "content_hash" TEXT NOT NULL DEFAULT '',
  "track_mode" TEXT NOT NULL DEFAULT 'pin',
  "share_mode" TEXT NOT NULL DEFAULT 'private',
  "deprecated_at" TEXT,
  "deprecation_note" TEXT,
  "superseded_by_id" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "tool_rate_limit_buckets" (
  "id" TEXT,
  "tool_name" TEXT NOT NULL,
  "scope_key" TEXT NOT NULL,
  "window_start" TEXT NOT NULL,
  "count" BIGINT NOT NULL DEFAULT 0,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "tool_registry" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "package_name" TEXT NOT NULL,
  "version" TEXT NOT NULL DEFAULT '1.0',
  "category" TEXT NOT NULL DEFAULT 'custom',
  "risk_level" TEXT NOT NULL DEFAULT 'low',
  "tags" TEXT,
  "config" TEXT,
  "requires_approval" BIGINT NOT NULL DEFAULT 0,
  "max_execution_ms" BIGINT,
  "rate_limit_per_min" BIGINT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "traces" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "chat_id" TEXT,
  "message_id" TEXT,
  "trace_id" TEXT NOT NULL,
  "span_id" TEXT NOT NULL,
  "parent_span_id" TEXT,
  "name" TEXT NOT NULL,
  "start_time" BIGINT NOT NULL,
  "end_time" BIGINT,
  "status" TEXT,
  "attributes" TEXT,
  "events" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("chat_id") REFERENCES "chats" ("id") ON DELETE CASCADE,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id")
);

CREATE TABLE IF NOT EXISTS "trigger_definitions" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "trigger_type" TEXT NOT NULL,
  "expression" TEXT,
  "config" TEXT,
  "target_workflow" TEXT,
  "status" TEXT NOT NULL DEFAULT 'active',
  "last_fired_at" TEXT,
  "fire_count" BIGINT NOT NULL DEFAULT 0,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "triggers" (
  "id" TEXT,
  "key" TEXT NOT NULL,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "source_kind" TEXT NOT NULL,
  "source_config" TEXT NOT NULL DEFAULT '{}',
  "filter_expr" TEXT,
  "target_kind" TEXT NOT NULL,
  "target_config" TEXT NOT NULL DEFAULT '{}',
  "input_map" TEXT,
  "rate_limit_per_minute" BIGINT,
  "metadata" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "trigger_invocations" (
  "id" TEXT,
  "trigger_id" TEXT NOT NULL,
  "fired_at" TEXT NOT NULL,
  "source_kind" TEXT NOT NULL,
  "status" TEXT NOT NULL,
  "target_ref" TEXT,
  "error_message" TEXT,
  "source_event" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("trigger_id") REFERENCES "triggers" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "user_devices" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "channel" TEXT NOT NULL,
  "token" TEXT NOT NULL,
  "label" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "user_invitations" (
  "id" TEXT,
  "email" TEXT NOT NULL,
  "persona" TEXT NOT NULL DEFAULT 'tenant_user',
  "token_hash" TEXT NOT NULL,
  "invited_by" TEXT NOT NULL,
  "expires_at" TEXT NOT NULL,
  "used_at" TEXT,
  "used_by" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("used_by") REFERENCES "users" ("id"),
  FOREIGN KEY ("invited_by") REFERENCES "users" ("id")
);

CREATE TABLE IF NOT EXISTS "user_mcp_tokens" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "name" TEXT NOT NULL DEFAULT 'MCP token',
  "token_hash" TEXT NOT NULL,
  "token_prefix" TEXT NOT NULL DEFAULT '',
  "scope" TEXT NOT NULL DEFAULT 'readwrite',
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "last_used_at" TEXT,
  "expires_at" TEXT,
  "service" TEXT NOT NULL DEFAULT 'notes',
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "user_notification_prefs" (
  "user_id" TEXT NOT NULL,
  "event_key" TEXT NOT NULL,
  "in_app" BIGINT NOT NULL DEFAULT 1,
  "email" BIGINT NOT NULL DEFAULT 0,
  "push" BIGINT NOT NULL DEFAULT 0,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("user_id", "event_key")
);

CREATE TABLE IF NOT EXISTS "user_preferences" (
  "user_id" TEXT,
  "default_mode" TEXT NOT NULL DEFAULT 'agent',
  "theme" TEXT NOT NULL DEFAULT 'light',
  "show_process_card" BIGINT NOT NULL DEFAULT 1,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "notes_image_language" TEXT NOT NULL DEFAULT 'en',
  "display_name" TEXT,
  "pronouns" TEXT,
  "role_title" TEXT,
  "working_hours" TEXT,
  "about" TEXT,
  "status_text" TEXT,
  "status_emoji" TEXT,
  "language" TEXT NOT NULL DEFAULT 'en-US',
  "timezone" TEXT,
  "date_format" TEXT NOT NULL DEFAULT 'D MMM YYYY',
  "week_start" TEXT NOT NULL DEFAULT 'monday',
  "ui_variant" TEXT NOT NULL DEFAULT 'pro',
  PRIMARY KEY ("user_id"),
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "user_prompt_suggestions" (
  "user_id" TEXT,
  "tenant_id" TEXT,
  "prompts_json" TEXT NOT NULL DEFAULT '[]',
  "generated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("user_id")
);

CREATE TABLE IF NOT EXISTS "user_run_events" (
  "id" TEXT,
  "run_id" TEXT NOT NULL,
  "sequence" BIGINT NOT NULL,
  "kind" TEXT NOT NULL,
  "payload" TEXT NOT NULL DEFAULT '{}',
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("run_id") REFERENCES "user_runs" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "validation_rules" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "rule_type" TEXT NOT NULL DEFAULT 'required',
  "target" TEXT NOT NULL DEFAULT 'agent-config',
  "condition" TEXT,
  "severity" TEXT NOT NULL DEFAULT 'error',
  "message" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "voice_configs" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "stt_provider" TEXT NOT NULL DEFAULT 'openai',
  "stt_model" TEXT NOT NULL DEFAULT 'whisper-1',
  "stt_language" TEXT,
  "tts_provider" TEXT NOT NULL DEFAULT 'openai',
  "tts_model" TEXT NOT NULL DEFAULT 'tts-1',
  "tts_voice" TEXT NOT NULL DEFAULT 'alloy',
  "tts_speed" DOUBLE PRECISION NOT NULL DEFAULT 1.0,
  "tts_format" TEXT NOT NULL DEFAULT 'mp3',
  "enabled_tools" TEXT,
  "mode" TEXT NOT NULL DEFAULT 'agent',
  "guardrail_policy" TEXT,
  "cost_policy" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "pipeline_mode" TEXT NOT NULL DEFAULT 'chained',
  "realtime_model" TEXT NOT NULL DEFAULT 'gpt-realtime-2',
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "voice_session_events" (
  "id" TEXT,
  "session_id" TEXT NOT NULL,
  "user_id" TEXT NOT NULL,
  "turn_index" BIGINT NOT NULL DEFAULT 0,
  "event_type" TEXT NOT NULL,
  "input_text" TEXT,
  "output_text" TEXT,
  "audio_bytes_in" BIGINT,
  "audio_bytes_out" BIGINT,
  "stt_provider" TEXT,
  "stt_model" TEXT,
  "tts_provider" TEXT,
  "tts_model" TEXT,
  "tts_voice" TEXT,
  "llm_provider" TEXT,
  "llm_model" TEXT,
  "prompt_tokens" BIGINT,
  "completion_tokens" BIGINT,
  "duration_ms" BIGINT,
  "cost_usd" DOUBLE PRECISION,
  "error" TEXT,
  "guardrail_decision" TEXT,
  "trace_id" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "voice_sessions" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "tenant_id" TEXT,
  "chat_id" TEXT NOT NULL,
  "status" TEXT NOT NULL DEFAULT 'active',
  "config_snapshot" TEXT NOT NULL,
  "total_turns" BIGINT NOT NULL DEFAULT 0,
  "total_stt_ms" BIGINT NOT NULL DEFAULT 0,
  "total_tts_ms" BIGINT NOT NULL DEFAULT 0,
  "total_llm_ms" BIGINT NOT NULL DEFAULT 0,
  "total_cost_usd" DOUBLE PRECISION NOT NULL DEFAULT 0,
  "total_audio_bytes" BIGINT NOT NULL DEFAULT 0,
  "ws_connected" BIGINT NOT NULL DEFAULT 0,
  "last_active_at" TEXT,
  "ended_at" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "weavenotes_settings" (
  "id" TEXT DEFAULT 'global',
  "default_theme" TEXT NOT NULL DEFAULT 'pro',
  "agency_color_enabled" BIGINT NOT NULL DEFAULT 1,
  "ai_suggestions_require_approval" BIGINT NOT NULL DEFAULT 1,
  "activity_tracking_enabled" BIGINT NOT NULL DEFAULT 1,
  "activity_retention_days" BIGINT NOT NULL DEFAULT 90,
  "max_ai_tokens_per_edit" BIGINT NOT NULL DEFAULT 4000,
  "local_model_for_sensitive" BIGINT NOT NULL DEFAULT 0,
  "enabled_ai_tools" TEXT NOT NULL DEFAULT '[]',
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "live_cursors_enabled" BIGINT NOT NULL DEFAULT 1,
  "ai_presence_enabled" BIGINT NOT NULL DEFAULT 1,
  "diagrams_enabled" BIGINT NOT NULL DEFAULT 1,
  "ink_enabled" BIGINT NOT NULL DEFAULT 1,
  "illustration_enabled" BIGINT NOT NULL DEFAULT 1,
  "image_generation_enabled" BIGINT NOT NULL DEFAULT 0,
  "image_model" TEXT NOT NULL DEFAULT 'gpt-image-1',
  "flashcards_enabled" BIGINT NOT NULL DEFAULT 1,
  "daily_new_card_limit" BIGINT NOT NULL DEFAULT 20,
  "mobile_offline_enabled" BIGINT NOT NULL DEFAULT 1,
  "mobile_ink_enabled" BIGINT NOT NULL DEFAULT 1,
  "mobile_offline_note_limit" BIGINT NOT NULL DEFAULT 200,
  "desktop_offline_enabled" BIGINT NOT NULL DEFAULT 1,
  "quick_capture_enabled" BIGINT NOT NULL DEFAULT 1,
  "desktop_offline_note_limit" BIGINT NOT NULL DEFAULT 500,
  "export_enabled" BIGINT NOT NULL DEFAULT 1,
  "allowed_export_formats" TEXT NOT NULL DEFAULT '["markdown","html","word","json"]',
  "image_search_enabled" BIGINT NOT NULL DEFAULT 1,
  "image_search_provider" TEXT NOT NULL DEFAULT 'openverse',
  "image_search_allowed_licenses" TEXT NOT NULL DEFAULT '["cc0","pdm","by","by-sa","unsplash","pexels","pixabay"]',
  "image_search_require_attribution" BIGINT NOT NULL DEFAULT 1,
  "ai_rate_per_min_per_user" BIGINT NOT NULL DEFAULT 30,
  "visual_verify_enabled" BIGINT NOT NULL DEFAULT 1,
  "visual_verify_threshold" DOUBLE PRECISION NOT NULL DEFAULT 0.7,
  "visual_verify_max_retries" BIGINT NOT NULL DEFAULT 2,
  "image_verify_enabled" BIGINT NOT NULL DEFAULT 1,
  "image_verify_min_confidence" DOUBLE PRECISION NOT NULL DEFAULT 0.7,
  "citations_enabled" BIGINT NOT NULL DEFAULT 1,
  "citation_max_sources" BIGINT NOT NULL DEFAULT 6,
  "fsrs_enabled" BIGINT NOT NULL DEFAULT 1,
  "fsrs_target_retention" DOUBLE PRECISION NOT NULL DEFAULT 0.9,
  "translate_enabled" BIGINT NOT NULL DEFAULT 1,
  "query_expansion_enabled" BIGINT NOT NULL DEFAULT 1,
  "query_expansion_variants" BIGINT NOT NULL DEFAULT 3,
  "db_autofill_web_search" BIGINT NOT NULL DEFAULT 1,
  "db_autofill_redact_pii" BIGINT NOT NULL DEFAULT 1,
  "image_provenance_enabled" BIGINT NOT NULL DEFAULT 1,
  "scheduled_agents_enabled" BIGINT NOT NULL DEFAULT 1,
  "scheduled_agent_max_token_budget" BIGINT NOT NULL DEFAULT 20000,
  "scheduled_agent_max_per_user" BIGINT NOT NULL DEFAULT 10,
  "mcp_notes_enabled" BIGINT NOT NULL DEFAULT 1,
  "mcp_notes_allow_writes" BIGINT NOT NULL DEFAULT 1,
  "proactive_linking_enabled" BIGINT NOT NULL DEFAULT 1,
  "entity_resolution_enabled" BIGINT NOT NULL DEFAULT 1,
  "embedding_batch_size" BIGINT NOT NULL DEFAULT 16,
  "voice_capture_enabled" BIGINT NOT NULL DEFAULT 1,
  "store_audio" BIGINT NOT NULL DEFAULT 0,
  "transcription_language" TEXT NOT NULL DEFAULT '',
  "transcription_model" TEXT NOT NULL DEFAULT 'whisper-1',
  "max_recording_seconds" BIGINT NOT NULL DEFAULT 3600,
  "background_memory_enabled" BIGINT NOT NULL DEFAULT 1,
  "memory_importance_threshold" DOUBLE PRECISION NOT NULL DEFAULT 0.3,
  "memory_max_per_note" BIGINT NOT NULL DEFAULT 8,
  "memory_recall_count" BIGINT NOT NULL DEFAULT 5,
  "memory_decay_half_life_days" BIGINT NOT NULL DEFAULT 30,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "webauthn_challenges" (
  "id" TEXT,
  "user_id" TEXT,
  "challenge" TEXT NOT NULL,
  "type" TEXT NOT NULL,
  "used" BIGINT NOT NULL DEFAULT 0,
  "expires_at" TEXT NOT NULL,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "webhook_endpoints" (
  "id" TEXT,
  "tenant_id" TEXT,
  "user_id" TEXT NOT NULL,
  "url" TEXT NOT NULL,
  "signing_secret" TEXT NOT NULL,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" BIGINT NOT NULL,
  "revoked_at" BIGINT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "website_credentials" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "site_name" TEXT NOT NULL,
  "site_url_pattern" TEXT NOT NULL,
  "auth_method" TEXT NOT NULL DEFAULT 'form_fill',
  "credentials_encrypted" TEXT NOT NULL,
  "encryption_iv" TEXT NOT NULL,
  "last_used_at" TEXT,
  "status" TEXT NOT NULL DEFAULT 'active',
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "widget_configs" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "widget_type" TEXT NOT NULL DEFAULT 'table',
  "default_options" TEXT,
  "allowed_contexts" TEXT,
  "max_data_points" BIGINT DEFAULT 1000,
  "refresh_interval_ms" BIGINT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "worker_agents" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "display_name" TEXT,
  "job_profile" TEXT,
  "description" TEXT NOT NULL DEFAULT '',
  "system_prompt" TEXT NOT NULL DEFAULT '',
  "tool_names" TEXT NOT NULL DEFAULT '[]',
  "persona" TEXT NOT NULL DEFAULT 'agent_worker',
  "trigger_patterns" TEXT,
  "task_contract_id" TEXT,
  "max_retries" BIGINT NOT NULL DEFAULT 0,
  "priority" BIGINT NOT NULL DEFAULT 0,
  "category" TEXT NOT NULL DEFAULT 'general',
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "agentic_scope" TEXT NOT NULL DEFAULT 'system',
  "realm" TEXT NOT NULL DEFAULT 'global',
  "owner_tenant_id" TEXT,
  "logical_key" TEXT,
  "origin_id" TEXT,
  "origin_hash" TEXT,
  "content_hash" TEXT NOT NULL DEFAULT '',
  "track_mode" TEXT NOT NULL DEFAULT 'pin',
  "share_mode" TEXT NOT NULL DEFAULT 'private',
  "deprecated_at" TEXT,
  "deprecation_note" TEXT,
  "superseded_by_id" TEXT,
  PRIMARY KEY ("id"),
  FOREIGN KEY ("task_contract_id") REFERENCES "task_contracts" ("id")
);

CREATE TABLE IF NOT EXISTS "workflow_runs" (
  "id" TEXT,
  "workflow_id" TEXT NOT NULL,
  "status" TEXT NOT NULL DEFAULT 'pending',
  "state" TEXT NOT NULL DEFAULT '{}',
  "input" TEXT,
  "error" TEXT,
  "started_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "completed_at" TEXT,
  "cost_total" DOUBLE PRECISION NOT NULL DEFAULT 0,
  "metadata" TEXT,
  "trace_id" TEXT,
  "tenant_id" TEXT,
  "parent_run_id" TEXT,
  "child_run_ids" TEXT,
  "priority" BIGINT NOT NULL DEFAULT 0,
  "cost_breakdown" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "workflow_checkpoints" (
  "id" TEXT,
  "run_id" TEXT NOT NULL,
  "workflow_id" TEXT NOT NULL,
  "step_id" TEXT NOT NULL,
  "state" TEXT NOT NULL DEFAULT '{}',
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("run_id") REFERENCES "workflow_runs" ("id") ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS "workflow_defs" (
  "id" TEXT,
  "name" TEXT NOT NULL,
  "description" TEXT,
  "version" TEXT NOT NULL DEFAULT '1.0',
  "steps" TEXT NOT NULL,
  "entry_step_id" TEXT NOT NULL,
  "metadata" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "realm" TEXT NOT NULL DEFAULT 'global',
  "owner_tenant_id" TEXT,
  "logical_key" TEXT,
  "origin_id" TEXT,
  "origin_hash" TEXT,
  "content_hash" TEXT NOT NULL DEFAULT '',
  "track_mode" TEXT NOT NULL DEFAULT 'pin',
  "share_mode" TEXT NOT NULL DEFAULT 'private',
  "deprecated_at" TEXT,
  "deprecation_note" TEXT,
  "superseded_by_id" TEXT,
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "workflow_events" (
  "id" TEXT,
  "run_id" TEXT NOT NULL,
  "workflow_id" TEXT NOT NULL,
  "type" TEXT NOT NULL,
  "step_id" TEXT,
  "timestamp" TEXT NOT NULL,
  "trace_id" TEXT,
  "tenant_id" TEXT,
  "caused_by" TEXT,
  "data" TEXT,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "workflow_handler_kinds" (
  "id" TEXT,
  "kind" TEXT NOT NULL,
  "description" TEXT,
  "config_schema" TEXT,
  "enabled" BIGINT NOT NULL DEFAULT 1,
  "source" TEXT NOT NULL DEFAULT 'builtin',
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "workflow_payloads" (
  "key" TEXT,
  "run_id" TEXT NOT NULL,
  "step_id" TEXT NOT NULL,
  "data" TEXT NOT NULL,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("key")
);

CREATE TABLE IF NOT EXISTS "workflow_rate_limits" (
  "workflow_id" TEXT,
  "tokens" DOUBLE PRECISION NOT NULL,
  "last_refill_ms" BIGINT NOT NULL,
  "updated_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("workflow_id")
);

CREATE TABLE IF NOT EXISTS "workflow_run_queue" (
  "id" TEXT,
  "run_id" TEXT NOT NULL,
  "workflow_id" TEXT NOT NULL,
  "input" TEXT NOT NULL DEFAULT '{}',
  "priority" BIGINT NOT NULL DEFAULT 0,
  "queued_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  "opts" TEXT NOT NULL DEFAULT '{}',
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "workflow_sleeps" (
  "run_id" TEXT,
  "wake_at" BIGINT NOT NULL,
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("run_id")
);

CREATE TABLE IF NOT EXISTS "workflow_spans" (
  "id" TEXT,
  "run_id" TEXT NOT NULL,
  "workflow_id" TEXT NOT NULL,
  "step_id" TEXT NOT NULL,
  "handler_kind" TEXT NOT NULL,
  "handler_key" TEXT NOT NULL,
  "started_at" BIGINT NOT NULL,
  "completed_at" BIGINT NOT NULL,
  "duration_ms" BIGINT NOT NULL,
  "status" TEXT NOT NULL,
  "retry_count" BIGINT NOT NULL DEFAULT 0,
  "cost_usd" DOUBLE PRECISION NOT NULL DEFAULT 0,
  "error" TEXT,
  "attributes" TEXT NOT NULL DEFAULT '{}',
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "workflow_step_locks" (
  "run_id" TEXT NOT NULL,
  "step_id" TEXT NOT NULL,
  "state" TEXT NOT NULL DEFAULT 'locked',
  "locked_at" TEXT NOT NULL,
  "done_at" TEXT,
  "output" TEXT,
  PRIMARY KEY ("run_id", "step_id")
);

CREATE TABLE IF NOT EXISTS "working_memory_snapshots" (
  "id" TEXT,
  "user_id" TEXT NOT NULL,
  "chat_id" TEXT,
  "agent_id" TEXT NOT NULL DEFAULT 'default',
  "content" TEXT NOT NULL DEFAULT '{}',
  "created_at" TEXT NOT NULL DEFAULT to_char((now() at time zone 'utc'), 'YYYY-MM-DD HH24:MI:SS'),
  PRIMARY KEY ("id"),
  FOREIGN KEY ("chat_id") REFERENCES "chats" ("id") ON DELETE SET NULL,
  FOREIGN KEY ("user_id") REFERENCES "users" ("id") ON DELETE CASCADE
);

-- Indexes & UNIQUE constraints
CREATE INDEX IF NOT EXISTS idx_a2a_skills_enabled_order ON a2a_skills(enabled, sort_order ASC);
CREATE INDEX IF NOT EXISTS idx_a2a_tasks_status_timestamp
  ON a2a_tasks(status_timestamp DESC);
CREATE INDEX IF NOT EXISTS idx_a2a_tasks_status_state
  ON a2a_tasks(status_state);
CREATE INDEX IF NOT EXISTS idx_a2a_tasks_context_id
  ON a2a_tasks(context_id);
CREATE INDEX IF NOT EXISTS idx_agenda_items_user_kind  ON agenda_items(user_id, kind);
CREATE INDEX IF NOT EXISTS idx_agenda_items_user_start ON agenda_items(user_id, start_at);
CREATE INDEX IF NOT EXISTS idx_checkpoints_status ON agent_checkpoints(chat_id, status);
CREATE INDEX IF NOT EXISTS idx_checkpoints_agent ON agent_checkpoints(agent_name);
CREATE INDEX IF NOT EXISTS idx_checkpoints_user ON agent_checkpoints(user_id);
CREATE INDEX IF NOT EXISTS idx_checkpoints_chat ON agent_checkpoints(chat_id);
CREATE INDEX IF NOT EXISTS idx_compliance_tool ON agent_compliance_audit(tool_name, outcome);
CREATE INDEX IF NOT EXISTS idx_compliance_subject ON agent_compliance_audit(subject_id);
CREATE INDEX IF NOT EXISTS idx_compliance_chat ON agent_compliance_audit(chat_id);
CREATE INDEX IF NOT EXISTS idx_cost_ledger_agent ON agent_cost_ledger(agent_id);
CREATE INDEX IF NOT EXISTS idx_cost_ledger_run ON agent_cost_ledger(run_id);
CREATE INDEX IF NOT EXISTS idx_cost_ledger_chat ON agent_cost_ledger(chat_id);
CREATE INDEX IF NOT EXISTS idx_eval_pipeline_accepted ON agent_eval_pipeline_runs(chat_id, accepted);
CREATE INDEX IF NOT EXISTS idx_eval_pipeline_run ON agent_eval_pipeline_runs(run_id);
CREATE INDEX IF NOT EXISTS idx_eval_pipeline_chat ON agent_eval_pipeline_runs(chat_id);
CREATE UNIQUE INDEX IF NOT EXISTS idx_graph_edges_unique ON agent_graph_edges(chat_id, source_entity_id, target_entity_id, relationship_type);
CREATE INDEX IF NOT EXISTS idx_graph_edges_type ON agent_graph_edges(chat_id, relationship_type);
CREATE INDEX IF NOT EXISTS idx_graph_edges_target ON agent_graph_edges(chat_id, target_entity_id);
CREATE INDEX IF NOT EXISTS idx_graph_edges_source ON agent_graph_edges(chat_id, source_entity_id);
CREATE INDEX IF NOT EXISTS idx_graph_edges_chat ON agent_graph_edges(chat_id);
CREATE INDEX IF NOT EXISTS idx_graph_nodes_name ON agent_graph_nodes(entity_name);
CREATE INDEX IF NOT EXISTS idx_graph_nodes_type ON agent_graph_nodes(chat_id, entity_type);
CREATE INDEX IF NOT EXISTS idx_graph_nodes_user ON agent_graph_nodes(user_id);
CREATE INDEX IF NOT EXISTS idx_graph_nodes_chat ON agent_graph_nodes(chat_id);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_agent_graph_nodes_chat_id_entity_id" ON "agent_graph_nodes" ("chat_id", "entity_id");
CREATE INDEX IF NOT EXISTS idx_handoff_agents ON agent_handoff_log(from_agent, to_agent);
CREATE INDEX IF NOT EXISTS idx_handoff_chat ON agent_handoff_log(chat_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_agent_output_schemas_name ON agent_output_schemas (name);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_agent_output_schemas_name" ON "agent_output_schemas" ("name");
CREATE INDEX IF NOT EXISTS idx_agent_structured_outputs_chat ON agent_structured_outputs (chat_id, created_at);
CREATE INDEX IF NOT EXISTS idx_agent_tools_agent ON agent_tools(agent_id);
CREATE INDEX IF NOT EXISTS idx_agents_default ON agents(is_default, enabled);
CREATE INDEX IF NOT EXISTS idx_agents_tenant_category ON agents(tenant_id, category, enabled);
CREATE INDEX IF NOT EXISTS idx_api_live_runs_user ON api_live_runs(user_id, status, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_artv_version        ON artifact_versions(artifact_id, version);
CREATE INDEX IF NOT EXISTS idx_artv_artifact       ON artifact_versions(artifact_id);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_artifact_versions_artifact_id_version" ON "artifact_versions" ("artifact_id", "version");
CREATE INDEX IF NOT EXISTS idx_artifacts_tenant ON artifacts(tenant_id) WHERE tenant_id IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_artifacts_streaming
      ON artifacts(streaming_status)
     WHERE streaming_status IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_artifacts_created   ON artifacts(created_at DESC);
CREATE INDEX IF NOT EXISTS idx_artifacts_policy    ON artifacts(policy_id);
CREATE INDEX IF NOT EXISTS idx_artifacts_scope     ON artifacts(scope);
CREATE INDEX IF NOT EXISTS idx_artifacts_type      ON artifacts(type);
CREATE INDEX IF NOT EXISTS idx_artifacts_run       ON artifacts(run_id);
CREATE INDEX IF NOT EXISTS idx_artifacts_agent     ON artifacts(agent_id);
CREATE INDEX IF NOT EXISTS idx_artifacts_user      ON artifacts(user_id);
CREATE INDEX IF NOT EXISTS idx_artifacts_session   ON artifacts(session_id);
CREATE INDEX IF NOT EXISTS idx_cache_invalidation_trigger ON cache_invalidation_rules(trigger) WHERE enabled = 1;
CREATE INDEX IF NOT EXISTS idx_cache_metrics_window ON cache_metrics(window_start DESC);
CREATE INDEX IF NOT EXISTS idx_capability_pack_experiments_key ON capability_pack_experiments(pack_key, enabled);
CREATE INDEX IF NOT EXISTS idx_capability_pack_installations_pack ON capability_pack_installations(pack_id, installed_at DESC);
CREATE INDEX IF NOT EXISTS idx_capability_packs_key ON capability_packs(pack_key, status);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_capability_packs_pack_key_version" ON "capability_packs" ("pack_key", "version");
CREATE INDEX IF NOT EXISTS idx_capability_policy_bindings_lookup ON capability_policy_bindings(binding_kind, binding_ref, policy_kind, enabled);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_capability_policy_bindings_binding_kind_binding_ref_polic" ON "capability_policy_bindings" ("binding_kind", "binding_ref", "policy_kind");
CREATE INDEX IF NOT EXISTS idx_chats_user_id ON chats(user_id, updated_at DESC);
CREATE INDEX IF NOT EXISTS idx_coedit_docs_run ON coedit_docs(run_id);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_coedit_docs_run_id" ON "coedit_docs" ("run_id");
CREATE UNIQUE INDEX IF NOT EXISTS idx_coedit_ops_unique ON coedit_ops(doc_id, op_site, op_counter);
CREATE INDEX IF NOT EXISTS idx_coedit_ops_doc  ON coedit_ops(doc_id, op_counter);
CREATE INDEX IF NOT EXISTS idx_cost_policies_deprecated ON cost_policies(deprecated_at);
CREATE INDEX IF NOT EXISTS idx_cost_policies_enabled ON cost_policies(enabled);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_cost_policies_key" ON "cost_policies" ("key");
CREATE INDEX IF NOT EXISTS idx_email_verif_expires ON email_verifications(expires_at);
CREATE INDEX IF NOT EXISTS idx_email_verif_user ON email_verifications(user_id);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_email_verifications_token_hash" ON "email_verifications" ("token_hash");
CREATE INDEX IF NOT EXISTS idx_encryption_audit_tenant ON encryption_audit(tenant_id, created_at);
CREATE INDEX IF NOT EXISTS idx_encryption_rewrite_tenant ON encryption_rewrite_progress(tenant_id, status);
CREATE INDEX IF NOT EXISTS idx_endpoint_health_circuit_state ON endpoint_health(circuit_state);
CREATE INDEX IF NOT EXISTS idx_endpoint_health_updated_at ON endpoint_health(updated_at);
CREATE INDEX IF NOT EXISTS idx_enterprise_connectors_enc_pending ON enterprise_connectors(credentials_encrypted) WHERE credentials_encrypted = 0;
CREATE UNIQUE INDEX IF NOT EXISTS "uq_entity_memory_user_id_entity_name" ON "entity_memory" ("user_id", "entity_name");
CREATE INDEX IF NOT EXISTS idx_episodic_consolidation
    ON episodic_memory(consolidated, user_id);
CREATE INDEX IF NOT EXISTS idx_episodic_user_created
    ON episodic_memory(user_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_eval_results_snapshot ON eval_results(settings_snapshot_at);
CREATE INDEX IF NOT EXISTS idx_eval_results_user ON eval_results(user_id, updated_at DESC);
CREATE INDEX IF NOT EXISTS idx_guardrail_revisions_guardrail
      ON guardrail_revisions (guardrail_id, created_at);
CREATE INDEX IF NOT EXISTS idx_guardrails_deprecated ON guardrails(deprecated_at);
CREATE INDEX IF NOT EXISTS idx_handoff_events_handoff ON handoff_events(handoff_id, at);
CREATE INDEX IF NOT EXISTS idx_hitl_run_status ON hitl_interrupt_requests(run_id, status);
CREATE INDEX IF NOT EXISTS idx_hitl_created_at ON hitl_interrupt_requests(created_at DESC);
CREATE INDEX IF NOT EXISTS idx_hitl_agent_pending ON hitl_interrupt_requests(agent_name, status) WHERE status = 'pending';
CREATE INDEX IF NOT EXISTS idx_hitl_chat_status ON hitl_interrupt_requests(chat_id, status);
CREATE INDEX IF NOT EXISTS idx_hv_agent_turn_hypothesis ON hv_agent_turn(hypothesis_id, created_at ASC);
CREATE INDEX IF NOT EXISTS idx_hv_evidence_hypothesis ON hv_evidence_event(hypothesis_id, created_at ASC);
CREATE INDEX IF NOT EXISTS idx_hv_evidence_event_hypothesis ON hv_evidence_event(hypothesis_id, created_at ASC);
CREATE INDEX IF NOT EXISTS idx_hv_hypothesis_status ON hv_hypothesis(tenant_id, status);
CREATE INDEX IF NOT EXISTS idx_hv_hypothesis_tenant ON hv_hypothesis(tenant_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_hv_sub_claim_hypothesis ON hv_sub_claim(hypothesis_id);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_hv_verdict_hypothesis_id" ON "hv_verdict" ("hypothesis_id");
CREATE INDEX IF NOT EXISTS idx_idempotency_expires_at ON idempotency_records(expires_at);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_idempotency_records_key" ON "idempotency_records" ("key");
CREATE INDEX IF NOT EXISTS idx_kaggle_approaches_status ON kaggle_approaches(status);
CREATE INDEX IF NOT EXISTS idx_kaggle_approaches_competition ON kaggle_approaches(competition_ref);
CREATE INDEX IF NOT EXISTS idx_kaggle_rubric_competition_ref ON kaggle_competition_rubric(competition_ref);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_kaggle_competition_rubric_tenant_id_competition_ref" ON "kaggle_competition_rubric" ("tenant_id", "competition_ref");
CREATE INDEX IF NOT EXISTS idx_kaggle_tracked_status ON kaggle_competitions_tracked(status);
CREATE INDEX IF NOT EXISTS idx_kaggle_tracked_competition ON kaggle_competitions_tracked(competition_ref);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_kaggle_competitions_tracked_tenant_id_competition_ref" ON "kaggle_competitions_tracked" ("tenant_id", "competition_ref");
CREATE INDEX IF NOT EXISTS idx_kaggle_discussion_posts_competition ON kaggle_discussion_posts(competition_ref);
CREATE INDEX IF NOT EXISTS idx_kaggle_discussion_posts_tenant ON kaggle_discussion_posts(tenant_id);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_kaggle_discussion_settings_tenant_id" ON "kaggle_discussion_settings" ("tenant_id");
CREATE INDEX IF NOT EXISTS idx_kaggle_lb_submission_id ON kaggle_leaderboard_scores(submission_id);
CREATE INDEX IF NOT EXISTS idx_kaggle_lb_competition_ref ON kaggle_leaderboard_scores(competition_ref);
CREATE INDEX IF NOT EXISTS idx_kaggle_lb_run_id ON kaggle_leaderboard_scores(run_id);
CREATE INDEX IF NOT EXISTS idx_kaggle_live_mesh_index_tenant ON kaggle_live_mesh_index(tenant_id);
CREATE INDEX IF NOT EXISTS idx_kaggle_run_artifacts_trace ON kaggle_run_artifacts(replay_trace_id);
CREATE INDEX IF NOT EXISTS idx_kaggle_run_artifacts_contract ON kaggle_run_artifacts(contract_id);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_kaggle_run_artifacts_run_id" ON "kaggle_run_artifacts" ("run_id");
CREATE INDEX IF NOT EXISTS idx_kaggle_runs_approach ON kaggle_runs(approach_id);
CREATE INDEX IF NOT EXISTS idx_kaggle_runs_status ON kaggle_runs(status);
CREATE INDEX IF NOT EXISTS idx_kaggle_runs_competition ON kaggle_runs(competition_ref);
CREATE INDEX IF NOT EXISTS idx_kaggle_validation_competition_ref ON kaggle_validation_results(competition_ref);
CREATE INDEX IF NOT EXISTS idx_kaggle_validation_verdict ON kaggle_validation_results(verdict);
CREATE INDEX IF NOT EXISTS idx_kaggle_validation_rubric_id ON kaggle_validation_results(rubric_id);
CREATE INDEX IF NOT EXISTS idx_kaggle_validation_run_id ON kaggle_validation_results(run_id);
CREATE INDEX IF NOT EXISTS idx_kgl_run_status ON kgl_competition_run(status);
CREATE INDEX IF NOT EXISTS idx_kgl_run_competition ON kgl_competition_run(competition_ref);
CREATE INDEX IF NOT EXISTS idx_kgl_run_tenant ON kgl_competition_run(tenant_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_kgl_event_step ON kgl_run_event(step_id);
CREATE INDEX IF NOT EXISTS idx_kgl_event_run ON kgl_run_event(run_id, id);
CREATE INDEX IF NOT EXISTS idx_kgl_step_status ON kgl_run_step(status);
CREATE INDEX IF NOT EXISTS idx_kgl_step_run ON kgl_run_step(run_id, step_index);
CREATE INDEX IF NOT EXISTS idx_la_entities_type_updated ON la_entities(entity_type, updated_at);
CREATE INDEX IF NOT EXISTS idx_live_agent_def_mesh ON live_agent_definitions(mesh_def_id, ordering);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_live_agent_definitions_mesh_def_id_role_key" ON "live_agent_definitions" ("mesh_def_id", "role_key");
CREATE INDEX IF NOT EXISTS idx_live_handler_bind_agent ON live_agent_handler_bindings(agent_id, enabled);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_live_agent_handler_bindings_agent_id_handler_kind" ON "live_agent_handler_bindings" ("agent_id", "handler_kind");
CREATE INDEX IF NOT EXISTS idx_live_tool_bind_agent ON live_agent_tool_bindings(agent_id, enabled);
CREATE INDEX IF NOT EXISTS idx_live_agents_mesh ON live_agents(mesh_id, status);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_live_agents_mesh_id_role_key" ON "live_agents" ("mesh_id", "role_key");
CREATE INDEX IF NOT EXISTS idx_live_artifact_configs_artifact
      ON live_artifact_configs(artifact_id);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_live_artifact_configs_artifact_id" ON "live_artifact_configs" ("artifact_id");
CREATE UNIQUE INDEX IF NOT EXISTS "uq_live_attention_policies_key" ON "live_attention_policies" ("key");
CREATE UNIQUE INDEX IF NOT EXISTS "uq_live_handler_kinds_kind" ON "live_handler_kinds" ("kind");
CREATE INDEX IF NOT EXISTS idx_live_mesh_def_enabled ON live_mesh_definitions(enabled);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_live_mesh_definitions_mesh_key" ON "live_mesh_definitions" ("mesh_key");
CREATE INDEX IF NOT EXISTS idx_live_edge_mesh ON live_mesh_delegation_edges(mesh_def_id, ordering);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_live_mesh_delegation_edges_mesh_def_id_from_role_key_to_r" ON "live_mesh_delegation_edges" ("mesh_def_id", "from_role_key", "to_role_key");
CREATE INDEX IF NOT EXISTS idx_live_meshes_def ON live_meshes(mesh_def_id);
CREATE INDEX IF NOT EXISTS idx_live_meshes_tenant ON live_meshes(tenant_id, status);
CREATE INDEX IF NOT EXISTS idx_live_run_events_run ON live_run_events(run_id, created_at, id);
CREATE INDEX IF NOT EXISTS idx_live_run_steps_mesh ON live_run_steps(mesh_id, status);
CREATE INDEX IF NOT EXISTS idx_live_run_steps_run ON live_run_steps(run_id, role_key);
CREATE INDEX IF NOT EXISTS idx_live_runs_mesh ON live_runs(mesh_id, status, started_at);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_live_runs_mesh_id_run_key" ON "live_runs" ("mesh_id", "run_key");
CREATE INDEX IF NOT EXISTS idx_mcp_gateway_clients_tenant ON mcp_gateway_clients(tenant_id);
CREATE INDEX IF NOT EXISTS idx_mcp_gateway_clients_expires ON mcp_gateway_clients(expires_at);
CREATE INDEX IF NOT EXISTS idx_mcp_gateway_clients_enabled ON mcp_gateway_clients(enabled);
CREATE INDEX IF NOT EXISTS idx_mcp_gateway_clients_hash ON mcp_gateway_clients(token_hash);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_mcp_gateway_clients_token_hash" ON "mcp_gateway_clients" ("token_hash");
CREATE UNIQUE INDEX IF NOT EXISTS "uq_mcp_gateway_clients_name" ON "mcp_gateway_clients" ("name");
CREATE INDEX IF NOT EXISTS idx_mcp_gateway_rate_buckets_tenant_client ON mcp_gateway_rate_buckets(tenant_id, client_id, window_start);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_mcp_gateway_rate_buckets_tenant_id_client_id_window_start" ON "mcp_gateway_rate_buckets" ("tenant_id", "client_id", "window_start");
CREATE INDEX IF NOT EXISTS idx_mcp_gateway_log_retention_tier ON mcp_gateway_request_log(retention_tier, created_at);
CREATE INDEX IF NOT EXISTS idx_mcp_gw_req_log_outcome ON mcp_gateway_request_log(outcome, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_mcp_gw_req_log_client ON mcp_gateway_request_log(client_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_mcp_gw_req_log_created ON mcp_gateway_request_log(created_at DESC);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_memory_settings_tenant_id" ON "memory_settings" ("tenant_id");
CREATE INDEX IF NOT EXISTS idx_mesh_contracts_mesh ON mesh_contracts(mesh_id, emitted_at DESC);
CREATE INDEX IF NOT EXISTS idx_mesh_contracts_run ON mesh_contracts(source_workflow_run_id);
CREATE INDEX IF NOT EXISTS idx_mesh_contracts_kind ON mesh_contracts(kind, emitted_at DESC);
CREATE INDEX IF NOT EXISTS idx_message_citations_msg ON message_citations(message_id);
CREATE INDEX IF NOT EXISTS idx_message_feedback_tenant ON message_feedback(tenant_id, created_at);
CREATE INDEX IF NOT EXISTS idx_feedback_signal ON message_feedback(signal, created_at);
CREATE INDEX IF NOT EXISTS idx_feedback_message ON message_feedback(message_id);
CREATE INDEX IF NOT EXISTS idx_message_variants_group ON message_variants(group_id, variant_index);
CREATE INDEX IF NOT EXISTS idx_messages_chat_id ON messages(chat_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_metrics_user ON metrics(user_id, chat_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_mined_proposals_status ON mined_skill_proposals(status, created_at);
CREATE INDEX IF NOT EXISTS idx_mode_labels_surface ON mode_labels(surface_id);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_mode_labels_surface_id_mode_key" ON "mode_labels" ("surface_id", "mode_key");
CREATE INDEX IF NOT EXISTS idx_capability_model ON model_capability_scores(model_id, provider);
CREATE INDEX IF NOT EXISTS idx_capability_lookup ON model_capability_scores(task_key, is_active, owner_tenant_id);
CREATE UNIQUE INDEX IF NOT EXISTS ux_model_capability_scores_logical_owner ON model_capability_scores(logical_key, COALESCE(owner_tenant_id, ''));
CREATE INDEX IF NOT EXISTS idx_model_capability_scores_deprecated ON model_capability_scores(deprecated_at);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_model_pricing_model_id_provider" ON "model_pricing" ("model_id", "provider");
CREATE INDEX IF NOT EXISTS idx_note_action_modes_tenant ON note_action_modes(tenant_id);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_note_action_modes_tenant_id_action_key" ON "note_action_modes" ("tenant_id", "action_key");
CREATE INDEX IF NOT EXISTS idx_note_activity_user ON note_activity(user_id, created_at);
CREATE INDEX IF NOT EXISTS idx_note_activity_note ON note_activity(note_id, created_at);
CREATE INDEX IF NOT EXISTS idx_note_coedit_docs_note ON note_coedit_docs(note_id);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_note_coedit_docs_note_id" ON "note_coedit_docs" ("note_id");
CREATE UNIQUE INDEX IF NOT EXISTS idx_note_coedit_ops_unique ON note_coedit_ops(doc_id, op_site, op_counter);
CREATE INDEX IF NOT EXISTS idx_note_coedit_ops_doc ON note_coedit_ops(doc_id, op_counter);
CREATE INDEX IF NOT EXISTS idx_note_comments_thread ON note_comments(thread_id);
CREATE INDEX IF NOT EXISTS idx_note_comments_note   ON note_comments(note_id, created_at);
CREATE INDEX IF NOT EXISTS idx_note_db_rows_db ON note_db_rows(database_id);
CREATE INDEX IF NOT EXISTS idx_note_embeddings_user ON note_embeddings(user_id);
CREATE INDEX IF NOT EXISTS idx_note_entities_canon ON note_entities(user_id, canonical_key);
CREATE INDEX IF NOT EXISTS idx_note_entities_user ON note_entities(user_id, name_key);
CREATE INDEX IF NOT EXISTS idx_note_entities_note ON note_entities(note_id);
CREATE INDEX IF NOT EXISTS idx_note_flashcards_due ON note_flashcards(owner_user_id, due_at);
CREATE INDEX IF NOT EXISTS idx_note_flashcards_note ON note_flashcards(note_id);
CREATE INDEX IF NOT EXISTS idx_note_links_target   ON note_links(target_kind, target_id);
CREATE INDEX IF NOT EXISTS idx_note_links_note     ON note_links(note_id);
CREATE INDEX IF NOT EXISTS idx_note_meetings_user ON note_meetings(user_id, created_at);
CREATE INDEX IF NOT EXISTS idx_note_meetings_note ON note_meetings(note_id);
CREATE INDEX IF NOT EXISTS idx_note_memory_state_user ON note_memory_state(user_id);
CREATE INDEX IF NOT EXISTS idx_note_relations_user ON note_relations(user_id);
CREATE INDEX IF NOT EXISTS idx_note_relations_note ON note_relations(note_id);
CREATE UNIQUE INDEX IF NOT EXISTS idx_note_share_tokens_hash ON note_share_tokens(token_hash);
CREATE INDEX IF NOT EXISTS idx_note_share_tokens_note ON note_share_tokens(note_id);
CREATE INDEX IF NOT EXISTS idx_note_shares_note ON note_shares(note_id);
CREATE INDEX IF NOT EXISTS idx_note_shares_user ON note_shares(user_id);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_note_shares_note_id_user_id" ON "note_shares" ("note_id", "user_id");
CREATE INDEX IF NOT EXISTS idx_note_suggestions_note ON note_suggestions(note_id, status);
CREATE INDEX IF NOT EXISTS idx_note_synced_source ON note_synced_blocks(source_note_id);
CREATE INDEX IF NOT EXISTS idx_note_synced_note   ON note_synced_blocks(note_id);
CREATE INDEX IF NOT EXISTS idx_note_versions_note ON note_versions(note_id, created_at);
CREATE INDEX IF NOT EXISTS idx_notes_user_parent ON notes(owner_user_id, parent_note_id);
CREATE UNIQUE INDEX IF NOT EXISTS idx_notification_feed_dedupe ON notification_feed(principal_id, dedupe_key) WHERE dedupe_key IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_notification_feed_principal ON notification_feed(principal_id, created_at);
CREATE INDEX IF NOT EXISTS idx_notification_outbox_drain ON notification_outbox(status, next_attempt_at);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_notification_outbox_run_id_user_id" ON "notification_outbox" ("run_id", "user_id");
CREATE UNIQUE INDEX IF NOT EXISTS "uq_notification_preferences_user_id" ON "notification_preferences" ("user_id");
CREATE INDEX IF NOT EXISTS idx_oauth_flow_states_expires_at ON oauth_flow_states(expires_at);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_oauth_flow_states_state_key" ON "oauth_flow_states" ("state_key");
CREATE UNIQUE INDEX IF NOT EXISTS "uq_oauth_linked_accounts_user_id_provider" ON "oauth_linked_accounts" ("user_id", "provider");
CREATE INDEX IF NOT EXISTS idx_passkey_cred_id ON passkey_credentials(credential_id);
CREATE INDEX IF NOT EXISTS idx_passkey_cred_user ON passkey_credentials(user_id);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_passkey_credentials_credential_id" ON "passkey_credentials" ("credential_id");
CREATE INDEX IF NOT EXISTS idx_procedural_user_status
    ON procedural_memory(user_id, status);
CREATE INDEX IF NOT EXISTS idx_prompt_contracts_deprecated ON prompt_contracts(deprecated_at);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_prompt_contracts_key" ON "prompt_contracts" ("key");
CREATE INDEX IF NOT EXISTS idx_eval_runs_status ON prompt_eval_runs(status, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_prompt_fragments_deprecated ON prompt_fragments(deprecated_at);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_prompt_fragments_key" ON "prompt_fragments" ("key");
CREATE INDEX IF NOT EXISTS idx_prompt_frameworks_deprecated ON prompt_frameworks(deprecated_at);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_prompt_frameworks_key" ON "prompt_frameworks" ("key");
CREATE INDEX IF NOT EXISTS idx_prompt_opt_runs_status ON prompt_optimization_runs(status, created_at DESC);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_prompt_optimizers_key" ON "prompt_optimizers" ("key");
CREATE INDEX IF NOT EXISTS idx_prompt_strategies_deprecated ON prompt_strategies(deprecated_at);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_prompt_strategies_key" ON "prompt_strategies" ("key");
CREATE INDEX IF NOT EXISTS idx_prompt_events_user ON prompt_suggestion_events(user_id, created_at);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_prompt_versions_prompt_id_version" ON "prompt_versions" ("prompt_id", "version");
CREATE INDEX IF NOT EXISTS idx_prompts_deprecated ON prompts(deprecated_at);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_provider_tool_adapters_provider" ON "provider_tool_adapters" ("provider");
CREATE UNIQUE INDEX IF NOT EXISTS ux_realm_proposals_pending_fork ON realm_proposals(fork_id) WHERE status = 'pending';
CREATE INDEX IF NOT EXISTS idx_realm_proposals_status ON realm_proposals(status, created_at);
CREATE INDEX IF NOT EXISTS ix_realm_tenant_state_family_key ON realm_tenant_state(family, logical_key);
CREATE UNIQUE INDEX IF NOT EXISTS ux_realm_tenant_state_tenant_key ON realm_tenant_state(tenant_id, family, logical_key);
CREATE INDEX IF NOT EXISTS ix_realm_versions_key_hash ON realm_versions(family, logical_key, content_hash);
CREATE INDEX IF NOT EXISTS ix_realm_versions_family_key ON realm_versions(family, logical_key);
CREATE UNIQUE INDEX IF NOT EXISTS ux_realm_versions_key_version ON realm_versions(family, logical_key, version);
CREATE INDEX IF NOT EXISTS idx_signals_tenant ON routing_capability_signals(tenant_id, created_at);
CREATE INDEX IF NOT EXISTS idx_signals_source ON routing_capability_signals(source, created_at);
CREATE INDEX IF NOT EXISTS idx_signals_lookup ON routing_capability_signals(model_id, provider, task_key, created_at);
CREATE INDEX IF NOT EXISTS idx_decision_agent ON routing_decision_traces(agent_id, decided_at);
CREATE INDEX IF NOT EXISTS idx_decision_tenant ON routing_decision_traces(tenant_id, decided_at);
CREATE INDEX IF NOT EXISTS idx_decision_task ON routing_decision_traces(task_key, decided_at);
CREATE INDEX IF NOT EXISTS idx_experiments_lookup ON routing_experiments(status, task_key, tenant_id);
CREATE INDEX IF NOT EXISTS idx_routing_policies_deprecated ON routing_policies(deprecated_at);
CREATE INDEX IF NOT EXISTS idx_surface_model ON routing_surface_items(model_id, provider, task_key);
CREATE INDEX IF NOT EXISTS idx_surface_status ON routing_surface_items(status, created_at);
CREATE INDEX IF NOT EXISTS idx_run_annotations_part ON run_annotations(run_id, part_id);
CREATE INDEX IF NOT EXISTS idx_run_annotations_run  ON run_annotations(run_id, created_at);
CREATE INDEX IF NOT EXISTS idx_run_comments_thread ON run_comments(thread_id);
CREATE INDEX IF NOT EXISTS idx_run_comments_run    ON run_comments(run_id, created_at);
CREATE INDEX IF NOT EXISTS idx_run_embeddings_user ON run_embeddings(user_id);
CREATE INDEX IF NOT EXISTS idx_run_presence_expires ON run_presence(expires_at);
CREATE INDEX IF NOT EXISTS idx_run_presence_run     ON run_presence(run_id);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_run_presence_run_id_user_id" ON "run_presence" ("run_id", "user_id");
CREATE INDEX IF NOT EXISTS idx_run_public_shares_run  ON run_public_shares(run_id);
CREATE INDEX IF NOT EXISTS idx_run_public_shares_hash ON run_public_shares(token_hash);
CREATE INDEX IF NOT EXISTS idx_run_subscriptions_user ON run_subscriptions(user_id);
CREATE INDEX IF NOT EXISTS idx_run_subscriptions_run  ON run_subscriptions(run_id);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_run_subscriptions_run_id_user_id" ON "run_subscriptions" ("run_id", "user_id");
CREATE INDEX IF NOT EXISTS idx_sched_runs_user ON scheduled_note_agent_runs(user_id, started_at);
CREATE INDEX IF NOT EXISTS idx_sched_runs_agent ON scheduled_note_agent_runs(agent_id, started_at);
CREATE INDEX IF NOT EXISTS idx_sched_agents_due ON scheduled_note_agents(next_run_at) WHERE enabled = 1 AND trigger_type = 'schedule';
CREATE INDEX IF NOT EXISTS idx_sched_agents_user ON scheduled_note_agents(user_id, enabled);
CREATE INDEX IF NOT EXISTS idx_scope_access_log_violation ON scope_access_log (allowed, event_type);
CREATE INDEX IF NOT EXISTS idx_scope_access_log_session ON scope_access_log (session_id);
CREATE INDEX IF NOT EXISTS idx_scope_access_log_created ON scope_access_log (created_at DESC);
CREATE INDEX IF NOT EXISTS idx_scope_cross_policies_to ON scope_cross_policies (to_scope);
CREATE INDEX IF NOT EXISTS idx_scope_cross_policies_from ON scope_cross_policies (from_scope);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_scope_cross_policies_from_scope_to_scope" ON "scope_cross_policies" ("from_scope", "to_scope");
CREATE INDEX IF NOT EXISTS idx_search_providers_enc_pending ON search_providers(credentials_encrypted) WHERE credentials_encrypted = 0;
CREATE INDEX IF NOT EXISTS idx_session_handoffs_sla   ON session_handoffs(state, expires_at);
CREATE INDEX IF NOT EXISTS idx_session_handoffs_to    ON session_handoffs(to_actor_id, state);
CREATE INDEX IF NOT EXISTS idx_session_handoffs_run   ON session_handoffs(run_id, created_at);
CREATE INDEX IF NOT EXISTS idx_session_participants_user    ON session_participants(user_id);
CREATE INDEX IF NOT EXISTS idx_session_participants_session ON session_participants(session_id);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_session_participants_session_id_user_id" ON "session_participants" ("session_id", "user_id");
CREATE INDEX IF NOT EXISTS idx_share_tokens_session ON session_share_tokens(session_id);
CREATE INDEX IF NOT EXISTS idx_share_tokens_hash    ON session_share_tokens(token_hash);
CREATE INDEX IF NOT EXISTS idx_sessions_expires ON sessions(expires_at);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_shared_sessions_run_id" ON "shared_sessions" ("run_id");
CREATE INDEX IF NOT EXISTS idx_skills_deprecated ON skills(deprecated_at);
CREATE INDEX IF NOT EXISTS idx_social_accounts_enc_pending ON social_accounts(credentials_encrypted) WHERE credentials_encrypted = 0;
CREATE UNIQUE INDEX IF NOT EXISTS "uq_sso_linked_accounts_user_id_identity_provider" ON "sso_linked_accounts" ("user_id", "identity_provider");
CREATE INDEX IF NOT EXISTS idx_starter_prompts_surface ON starter_prompts(surface_id);
CREATE INDEX IF NOT EXISTS idx_task_types_category ON task_type_definitions(category, enabled);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_task_type_definitions_task_key" ON "task_type_definitions" ("task_key");
CREATE UNIQUE INDEX IF NOT EXISTS "uq_task_type_tenant_overrides_tenant_id_task_key" ON "task_type_tenant_overrides" ("tenant_id", "task_key");
CREATE INDEX IF NOT EXISTS idx_tenant_artifact_settings_tenant ON tenant_artifact_settings(tenant_id);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_tenant_artifact_settings_tenant_id" ON "tenant_artifact_settings" ("tenant_id");
CREATE INDEX IF NOT EXISTS idx_attestation_tenant ON tenant_attestation_log(tenant_id, generated_at DESC);
CREATE INDEX IF NOT EXISTS idx_tenant_biks_tenant ON tenant_biks(tenant_id, status);
CREATE INDEX IF NOT EXISTS idx_break_glass_due ON tenant_break_glass_request(status, expires_at);
CREATE INDEX IF NOT EXISTS idx_break_glass_tenant ON tenant_break_glass_request(tenant_id, status);
CREATE INDEX IF NOT EXISTS idx_tenant_byok_fp ON tenant_byok_config(public_key_fingerprint);
CREATE INDEX IF NOT EXISTS idx_tenant_byok_status ON tenant_byok_config(status);
CREATE INDEX IF NOT EXISTS idx_tenant_deks_epoch ON tenant_deks(tenant_id, epoch);
CREATE INDEX IF NOT EXISTS idx_tenant_deks_tenant ON tenant_deks(tenant_id, status);
CREATE INDEX IF NOT EXISTS idx_tenant_deletion_requests_due ON tenant_deletion_requests(status, retention_until);
CREATE INDEX IF NOT EXISTS idx_tenant_deletion_requests_tenant ON tenant_deletion_requests(tenant_id, status);
CREATE INDEX IF NOT EXISTS idx_tenant_encryption_alert_tenant ON tenant_encryption_alert_config(tenant_id);
CREATE INDEX IF NOT EXISTS idx_tenant_governance_tenant ON tenant_governance(tenant_id);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_tenant_governance_tenant_id" ON "tenant_governance" ("tenant_id");
CREATE INDEX IF NOT EXISTS idx_tenant_keks_tenant ON tenant_keks(tenant_id, status);
CREATE INDEX IF NOT EXISTS ix_tenants_parent ON tenants(parent_tenant_id);
CREATE UNIQUE INDEX IF NOT EXISTS ux_tenants_path ON tenants(path);
CREATE INDEX IF NOT EXISTS idx_tool_approval_tool ON tool_approval_requests(tool_name, status);
CREATE INDEX IF NOT EXISTS idx_tool_approval_chat ON tool_approval_requests(chat_id, status);
CREATE INDEX IF NOT EXISTS idx_tool_audit_retention_tier ON tool_audit_events(retention_tier, created_at);
CREATE INDEX IF NOT EXISTS idx_tool_audit_created_at ON tool_audit_events(created_at);
CREATE INDEX IF NOT EXISTS idx_tool_audit_outcome ON tool_audit_events(outcome);
CREATE INDEX IF NOT EXISTS idx_tool_audit_chat_id ON tool_audit_events(chat_id);
CREATE INDEX IF NOT EXISTS idx_tool_audit_tool_name ON tool_audit_events(tool_name);
CREATE INDEX IF NOT EXISTS idx_tool_cache_tool ON tool_cache_policies(tool_name) WHERE enabled = 1;
CREATE UNIQUE INDEX IF NOT EXISTS "uq_tool_cache_policies_tool_name" ON "tool_cache_policies" ("tool_name");
CREATE UNIQUE INDEX IF NOT EXISTS "uq_tool_catalog_tool_key" ON "tool_catalog" ("tool_key");
CREATE INDEX IF NOT EXISTS idx_tool_credentials_enabled ON tool_credentials(enabled);
CREATE INDEX IF NOT EXISTS idx_tool_embeddings_model ON tool_embeddings(model_id);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_tool_embeddings_tool_key" ON "tool_embeddings" ("tool_key");
CREATE INDEX IF NOT EXISTS idx_tool_health_name_time ON tool_health_snapshots(tool_name, snapshot_at);
CREATE INDEX IF NOT EXISTS idx_tool_policies_deprecated ON tool_policies(deprecated_at);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_tool_policies_key" ON "tool_policies" ("key");
CREATE INDEX IF NOT EXISTS idx_tool_rate_buckets ON tool_rate_limit_buckets(tool_name, scope_key, window_start);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_tool_rate_limit_buckets_tool_name_scope_key_window_start" ON "tool_rate_limit_buckets" ("tool_name", "scope_key", "window_start");
CREATE INDEX IF NOT EXISTS idx_traces_user_id ON traces(user_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_traces_chat_id ON traces(chat_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_trigger_invocations_status ON trigger_invocations(status, fired_at DESC);
CREATE INDEX IF NOT EXISTS idx_trigger_invocations_trigger ON trigger_invocations(trigger_id, fired_at DESC);
CREATE INDEX IF NOT EXISTS idx_triggers_target_kind ON triggers(target_kind);
CREATE INDEX IF NOT EXISTS idx_triggers_source_kind ON triggers(source_kind, enabled);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_triggers_key" ON "triggers" ("key");
CREATE INDEX IF NOT EXISTS idx_user_devices_user_id ON user_devices(user_id);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_user_devices_user_id_token" ON "user_devices" ("user_id", "token");
CREATE INDEX IF NOT EXISTS idx_invitation_expires ON user_invitations(expires_at);
CREATE INDEX IF NOT EXISTS idx_invitation_email ON user_invitations(email);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_user_invitations_token_hash" ON "user_invitations" ("token_hash");
CREATE INDEX IF NOT EXISTS idx_user_mcp_tokens_hash ON user_mcp_tokens(token_hash);
CREATE INDEX IF NOT EXISTS idx_user_mcp_tokens_user ON user_mcp_tokens(user_id, enabled);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_user_mcp_tokens_token_hash" ON "user_mcp_tokens" ("token_hash");
CREATE INDEX IF NOT EXISTS idx_user_run_events_seq    ON user_run_events(run_id, sequence);
CREATE INDEX IF NOT EXISTS idx_user_run_events_run_id ON user_run_events(run_id);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_user_run_events_run_id_sequence" ON "user_run_events" ("run_id", "sequence");
CREATE INDEX IF NOT EXISTS idx_user_runs_created_at ON user_runs(created_at);
CREATE INDEX IF NOT EXISTS idx_user_runs_status     ON user_runs(status);
CREATE INDEX IF NOT EXISTS idx_user_runs_user_id    ON user_runs(user_id);
CREATE INDEX IF NOT EXISTS idx_users_mfa_enabled ON users(mfa_enabled);
CREATE INDEX IF NOT EXISTS idx_users_email_bidx ON users(email_bidx);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_users_email" ON "users" ("email");
CREATE INDEX IF NOT EXISTS idx_voice_configs_tenant ON voice_configs(tenant_id);
CREATE INDEX IF NOT EXISTS idx_voice_configs_user ON voice_configs(user_id);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_voice_configs_user_id" ON "voice_configs" ("user_id");
CREATE INDEX IF NOT EXISTS idx_vse_type    ON voice_session_events(event_type, session_id);
CREATE INDEX IF NOT EXISTS idx_vse_user    ON voice_session_events(user_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_vse_session ON voice_session_events(session_id, turn_index);
CREATE INDEX IF NOT EXISTS idx_voice_sessions_tenant ON voice_sessions(tenant_id, status);
CREATE INDEX IF NOT EXISTS idx_voice_sessions_chat   ON voice_sessions(chat_id);
CREATE INDEX IF NOT EXISTS idx_voice_sessions_user   ON voice_sessions(user_id, status);
CREATE INDEX IF NOT EXISTS idx_webauthn_challenges_expires ON webauthn_challenges(expires_at);
CREATE INDEX IF NOT EXISTS idx_webauthn_challenges_user ON webauthn_challenges(user_id, type);
CREATE INDEX IF NOT EXISTS idx_webhook_endpoints_user ON webhook_endpoints(user_id);
CREATE INDEX IF NOT EXISTS idx_worker_agents_deprecated ON worker_agents(deprecated_at);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_worker_agents_name" ON "worker_agents" ("name");
CREATE INDEX IF NOT EXISTS idx_workflow_checkpoints_run ON workflow_checkpoints(run_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_workflow_events_type ON workflow_events(type);
CREATE INDEX IF NOT EXISTS idx_workflow_events_run ON workflow_events(run_id);
CREATE INDEX IF NOT EXISTS idx_workflow_handler_kinds_kind ON workflow_handler_kinds(kind);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_workflow_handler_kinds_kind" ON "workflow_handler_kinds" ("kind");
CREATE INDEX IF NOT EXISTS idx_workflow_payloads_run ON workflow_payloads(run_id);
CREATE INDEX IF NOT EXISTS idx_workflow_run_queue_wf ON workflow_run_queue(workflow_id, priority DESC, queued_at ASC);
CREATE UNIQUE INDEX IF NOT EXISTS "uq_workflow_run_queue_run_id" ON "workflow_run_queue" ("run_id");
CREATE INDEX IF NOT EXISTS idx_workflow_runs_status ON workflow_runs(status);
CREATE INDEX IF NOT EXISTS idx_workflow_runs_parent ON workflow_runs(parent_run_id);
CREATE INDEX IF NOT EXISTS idx_workflow_runs_tenant ON workflow_runs(tenant_id);
CREATE INDEX IF NOT EXISTS idx_workflow_runs_trace ON workflow_runs(trace_id);
CREATE INDEX IF NOT EXISTS idx_workflow_sleeps_wake ON workflow_sleeps(wake_at);
CREATE INDEX IF NOT EXISTS idx_workflow_spans_step ON workflow_spans(run_id, step_id);
CREATE INDEX IF NOT EXISTS idx_workflow_spans_wf ON workflow_spans(workflow_id);
CREATE INDEX IF NOT EXISTS idx_workflow_spans_run ON workflow_spans(run_id);
CREATE INDEX IF NOT EXISTS idx_workflow_step_locks_run ON workflow_step_locks(run_id);
CREATE INDEX IF NOT EXISTS idx_working_memory_user_agent
    ON working_memory_snapshots(user_id, agent_id, created_at DESC);
`;
